//============================================================================================================================================
//                                                              API.SYMBOLINDEX
//============================================================================================================================================
// 🧩 Device image extents — claimed against a declared shape, viewed once, and carrying the layout each one stands in.

%format     symbolindex 1.0
%scope      folder
%path       Engine/SlateVulkan/Device/ImageSpace/Api
%layer      SlateVulkan
%sources    1
%symbols    19
%annotated  13/19
%cost       ✔️ low · 🚩 medium · 🔴 high (cost rises left to right)

//------------------------------------------------------------------------------------------------------------------------
//                                                        SOURCES
//------------------------------------------------------------------------------------------------------------------------

S ImageSpace.h | 188 lines | 5b68b393 | 19 sym | Device image extents — claimed against a declared shape, viewed once, and carrying the layout each one stands in.

//------------------------------------------------------------------------------------------------------------------------
//                                                   THE DECLARED SHAPE
//------------------------------------------------------------------------------------------------------------------------

V AbsentImage              | ImageSpace.h | 26      | -                         | -  | ?
    by    Source/ImageSpace.cpp, Source/RenderSchedule.cpp

E ImageIntent              | ImageSpace.h | 33-40   | contract                  | -  | What a claimant intends to do with an image, which is what its usage and its aspect follow from. source for `60`, and an image claimed for one and used for the other is a validation error at the recording site rather than at the claim — a long way from the declaration that caused it.
    has   ColourTarget     ImageIntent  [-]  ?
    has   DepthTarget      ImageIntent  [-]  ?
    has   ComputeWritable  ImageIntent  [-]  ?
    has   SampledOnly      ImageIntent  [-]  ?
    has   IntentCount      ImageIntent  [-]  ?
    by    Source/ImageSpace.cpp, Source/RenderSchedule.cpp
    note  🔴 Declared rather than derived from the format. D32 is a depth attachment for `16` and a sampled

T ImageShape               | ImageSpace.h | 46-54   | nonallocating,nonthrowing | -  | One image's declared shape, authored by the claimant and never inferred here. the claim because the chain's extents are the image's, and an image claimed flat cannot grow one.
    has   Format      VkFormat       [-]  ?
    has   Width       std::uint32_t  [-]  ?
    has   Height      std::uint32_t  [-]  ?
    has   LayerCount  std::uint32_t  [-]  ?
    has   LevelCount  std::uint32_t  [-]  ?
    has   Intent      ImageIntent    [-]  ?
    by    Api/RenderSchedule.h, Source/ImageSpace.cpp, Source/RenderSchedule.cpp
    note  ⚠️ `LevelCount` above one is the reduction chain `16` §2 walks for its predictor. It is declared at

//------------------------------------------------------------------------------------------------------------------------
//                                                   ONE CLAIMED IMAGE
//------------------------------------------------------------------------------------------------------------------------

T ImageClaim               | ImageSpace.h | 65-72   | nonallocating,nonthrowing | -  | What a claimant is handed — the image, the whole-image view, and where it currently stands. alone, because a recording that issues its own barrier has made a claim the record cannot see and the next transition then barriers from a layout the image is not in.
    has   Extent          VkImage        [-]  ?
    has   WholeView       VkImageView    [-]  ?
    has   StandingLayout  VkImageLayout  [-]  ?
    has   Shape           ImageShape     [-]  ?
    has   ImageOrdinal    std::uint32_t  [-]  ?
    by    Api/RenderSchedule.h, Source/AttachmentIndex.cpp, Source/ImageSpace.cpp, Source/OcclusionScheduler.cpp, Source/RenderSchedule.cpp
    note  🔴 `StandingLayout` is the component's record and not the caller's. It is amended by `Transition`

//------------------------------------------------------------------------------------------------------------------------
//                                                    THE IMAGE LEDGER
//------------------------------------------------------------------------------------------------------------------------

T ImageSpace               | ImageSpace.h | 85-186  | owning                    | -  | Every device image the engine holds, each sliced out of `ByteSpace` and each carrying its own layout. target set declares fifteen formats and their extent relations, and until something walked that declaration and claimed against it the table was a document rather than a target. one — `06` §7's gate, enforced by the caller passing the relation, never re-derived here.
    has   DeviceEdge    const VulkanExchange*       [-]  ?
    has   BackingBytes  ByteSpace*                  [-]  ?
    has   NamingEdge    const DiagnosticExtension*  [-]  ?
    has   Images        std::vector<HeldImage>      [-]  ?
    by    Api/OcclusionScheduler.h, Api/RenderSchedule.h, Api/WorkspaceSequence.h, Source/ImageSpace.cpp, Source/OcclusionScheduler.cpp, Source/RenderSchedule.cpp
    note  🔴 This is where `08` §2's "nothing claims a format, an extent or memory" is closed. The shared
    note  ⚠️ An extent change reclaims and re-claims every display-relative image and touches no absolute

F ImageSpace::~ImageSpace  | ImageSpace.h | 92      | destructor                | -  | ?

F ImageSpace::Construct    | ImageSpace.h | 104     | api,nonthrowing           | ✔️ | Takes the device and the byte extents every claimed image is sliced from. separate vendor objects, and the driver reports a view's error against the view — an unnamed view under a named image reports as an address beside a name, which reads as two objects.
    in    Exchange      const VulkanExchange&       [-]  the created device; borrowed and outlives this component
    in    BackingSpace  ByteSpace&                  [-]  where image bytes come from; borrowed and outlives this component
    in    Naming        const DiagnosticExtension&  [-]  names every claimed image and every view over it; borrowed and outlives this
    out   -             Outcome                     [-]  refuses with CapabilityAbsent when no device is active
    by    Api/AnalyticProjection.h, Api/AtmosphereIntegrator.h, Api/AttachmentIndex.h, Api/ByteSpace.h, Api/CameraProjection.h, Api/CommandSequence.h, (+66 more)
    note  🔴 `06` §7's diagnostic-name gate. The image and its views are named separately because they are

F ImageSpace::Claim        | ImageSpace.h | 117     | api,nonthrowing           | 🔴 | Claims one image of the declared shape, slices its bytes, and constructs its whole-image view. zero extent or a format the device declines for the declared intent vendor allocation nothing holds a reference to, and it is reclaimed only at device teardown.
    in    Declared  const ImageShape&  [-]  the shape; nothing about it is inferred from the format
    out   -         Outcome            [-]  refuses with ExtentExhausted when no bytes remain, ContentUnsupported for a
    post  the image stands in VK_IMAGE_LAYOUT_UNDEFINED and is transitioned before first use
    by    Api/ByteSpace.h, Api/DescriptorIndex.h, Api/RenderSchedule.h, Api/SpanSpace.h, Api/StrokeSpace.h, Api/TileSpace.h, (+15 more)
    note  🔴 Refused in full. An image whose bytes were claimed and whose view was declined leaves a

F ImageSpace::Transition   | ImageSpace.h | 129     | api,nonthrowing           | ✔️ | Records the barrier that carries one image from where it stands to where it is next read. derived from the declared reads and writes, and this is the one place it is recorded.
    in    Recorded         VkCommandBuffer                                                    [-]  the command being recorded into
    in    ImageOrdinal     std::uint32_t                                                      [-]  ?
    in    Arriving         VkImageLayout                                                      [-]  the layout the next recording requires
    in    ImageOrdinal[-]  the claim's ordinal; the record is amended, not the caller's copy  [-]  ?
    out   -                Outcome                                                            [-]  refuses with ContentUnsupported for an unclaimed ordinal
    post  the recorded layout is the arriving one; a repeat transition to the same layout is a no-op
    by    Source/ImageSpace.cpp, Source/OcclusionScheduler.cpp
    note  🔴 `08` §4: no contributing document issues a layout transition directly. The barrier is

F ImageSpace::Standing     | ImageSpace.h | 135     | api,nonthrowing           | ✔️ | The current record for one claimed image, including the layout the last transition left it in.
    in    ImageOrdinal  std::uint32_t  [-]  ?
    out   -             Outcome        [-]  refuses with ContentUnsupported for an unclaimed ordinal
    by    Api/ByteSpace.h, Api/CameraProjection.h, Api/ChartPartition.h, Api/CodeInterchange.h, Api/CycleScheduler.h, Api/DecalProjection.h, (+83 more)

F ImageSpace::LevelView    | ImageSpace.h | 145     | api,nonthrowing           | 🚩 | Constructs a view over one reduction level, for the chain `16` §2 walks a level at a time. holding a handle the vendor has already reused.
    in    ImageOrdinal  std::uint32_t  [-]  a claimed image whose LevelCount admits the level
    in    LevelOrdinal  std::uint32_t  [-]  the level; zero is the full extent
    out   -             Outcome        [-]  refuses with ContentUnsupported outside the declared level count
    by    Source/ImageSpace.cpp
    note  The view is owned here and reclaimed with the image. A caller destroying one leaves the ledger

F ImageSpace::Release      | ImageSpace.h | 151     | api,nonthrowing           | 🚩 | Destroys one image, every view over it, and returns its bytes.
    in    ImageOrdinal  std::uint32_t  [-]  ?
    out   -             void           [-]  ?
    pre   the device is idle, or no recording still in the rotation reads it
    by    Api/ByteSpace.h, Api/CodeInterchange.h, Api/GlyphDepot.h, Api/InstructionExchange.h, Api/PopulationIndex.h, Api/SpanSpace.h, (+13 more)

F ImageSpace::Reclaim      | ImageSpace.h | 157     | api,nonthrowing           | 🔴 | Releases every claimed image.
    out   -  void  [-]  ?
    pre   the device is idle
    by    Api/AttachmentIndex.h, Api/ByteSpace.h, Api/CodeInterchange.h, Api/CommandSequence.h, Api/CycleScheduler.h, Api/DepthReduction.h, (+79 more)

F ImageSpace::ClaimedCount | ImageSpace.h | 159     | -                         | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Api/DescriptorIndex.h, Api/SpanSpace.h, Api/StrokeSpace.h, Api/TileSpace.h, Source/ConsoleHost.cpp, Source/DescriptorIndex.cpp, (+6 more)

F ImageSpace::ClaimedBytes | ImageSpace.h | 160     | -                         | -  | ?
    out   -  VkDeviceSize  [-]  ?
    by    Api/ByteSpace.h, Api/SpanSpace.h, Source/ByteSpace.cpp, Source/ImageSpace.cpp, Source/SpanSpace.cpp, Source/WorkspaceSequence.cpp

T ImageSpace::HeldImage    | ImageSpace.h | 164-173 | -                         | -  | ?
    has   Extent          VkImage                   [-]  ?
    has   WholeView       VkImageView               [-]  ?
    has   LevelViews      std::vector<VkImageView>  [-]  ?
    has   Backing         ByteClaim                 [-]  ?
    has   Shape           ImageShape                [-]  ?
    has   StandingLayout  VkImageLayout             [-]  ?
    has   SlotOccupied    bool                      [-]  ?
    by    Source/ImageSpace.cpp

F ImageSpace::UsageOf      | ImageSpace.h | 176     | -                         | -  | What the declared intent requires of the image, as the vendor spells it.
    in    Intent  ImageIntent               [-]  ?
    out   -       static VkImageUsageFlags  [-]  ?
    by    Api/SpanSpace.h, Source/ImageSpace.cpp, Source/SpanSpace.cpp

F ImageSpace::AspectOf     | ImageSpace.h | 177     | -                         | -  | ?
    in    Intent  ImageIntent                [-]  ?
    out   -       static VkImageAspectFlags  [-]  ?
    by    Source/ImageSpace.cpp

F ImageSpace::NameOf       | ImageSpace.h | 180     | -                         | -  | What one declared intent is named in the driver's text, so a claim's name states what writes it.
    in    Intent  ImageIntent         [-]  ?
    out   -       static const char*  [-]  ?
    by    Api/SpanSpace.h, Source/ImageSpace.cpp, Source/SpanSpace.cpp
