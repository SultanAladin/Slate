//============================================================================================================================================
//                                                              API.SYMBOLINDEX
//============================================================================================================================================
// 🧩 Topology and imagery in, painted channels out — one contract, and intake that never repairs.

%format     symbolindex 1.0
%scope      folder
%path       Engine/SlateDocument/Document/AssetInterchange/Api
%layer      SlateDocument
%sources    1
%symbols    17
%annotated  14/17
%cost       ✔️ low · 🚩 medium · 🔴 high (cost rises left to right)

//------------------------------------------------------------------------------------------------------------------------
//                                                        SOURCES
//------------------------------------------------------------------------------------------------------------------------

S AssetInterchange.h | 225 lines | 4b4fc084 | 17 sym | Topology and imagery in, painted channels out — one contract, and intake that never repairs.

//------------------------------------------------------------------------------------------------------------------------
//                                                WHAT A CODEC HANDS OVER
//------------------------------------------------------------------------------------------------------------------------

T DecodedTopology                        | AssetInterchange.h | 33-45   | owning                        | -  | A decoded topology, faithful to the source and repaired in no respect. vertices, reverses winding or drops a degenerate face has produced a specification that no longer describes the file the artist supplied, and `38`'s guarantee that an index means the same thing afterwards is broken before `38` has run. `VectorInterchange` takes an `OutlineSpecification` in. The codec fills it; nothing here parses.
    has   Positions           std::vector<DocumentPosition>            [-]  ?
    has   Faces               std::vector<std::vector<std::uint32_t>>  [-]  ?
    has   CornerCoordinates   std::vector<DomainCoordinate>            [-]  ?
    has   Perpendiculars      std::vector<SurfaceDirection>            [-]  ?
    has   TangentBases        std::vector<TangentBasis>                [-]  ?
    has   MaterialEnrollment  std::vector<std::uint32_t>               [-]  ?
    has   UnsupportedNamed    std::vector<std::string>                 [-]  ?
    has   OriginPath          std::string                              [-]  ?
    has   UnitScale           double                                   [-]  ?
    has   UnitScaleDeclared   bool                                     [-]  ?
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  🔴 `50` §2 ①: intake **never repairs**. `38`'s non-mutation rule begins here — a codec that welds
    note  🚧 `10` §1's `TopologyCodec` is unbuilt, so this arrives already decoded — the same shape

T DecodedImage                           | AssetInterchange.h | 54-64   | owning                        | -  | A decoded image, its original retained. converted image is the defect where correcting an artist's mistake makes the image worse than the mistake did. precision loss with no origin anybody can point at afterwards.
    has   Original        std::vector<std::uint8_t>  [-]  ?
    has   OriginPath      std::string                [-]  ?
    has   Width           std::uint32_t              [-]  ?
    has   Height          std::uint32_t              [-]  ?
    has   ComponentCount  std::uint32_t              [-]  ?
    has   BitDepth        std::uint32_t              [-]  ?
    has   SpaceIdentity   std::uint32_t              [-]  ?
    has   SpaceDeclared   bool                       [-]  ?
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  🔴 `36` §3 and `50` §4: the original is retained so re-conversion is always **from it**. Converting a
    note  🔴 Bit depth is retained and never narrowed at intake. A sixteen-bit source narrowed on the way in is a

//------------------------------------------------------------------------------------------------------------------------
//                                                      THE EMISSION
//------------------------------------------------------------------------------------------------------------------------

E ComponentSlot                          | AssetInterchange.h | 72-79   | contract                      | -  | One component of an emitted image.
    has   Red             ComponentSlot  [-]  ?
    has   Green           ComponentSlot  [-]  ?
    has   Blue            ComponentSlot  [-]  ?
    has   Alpha           ComponentSlot  [-]  ?
    has   ComponentCount  ComponentSlot  [-]  ?
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp

T EmittedImage                           | AssetInterchange.h | 86-93   | owning                        | -  | One image an emission produces — its extent, its space, and which channel occupies which component. metalness into three components is one convention among many and the consumer decides which; a wrong arrangement is a shipped asset that renders as a plausible, wrong surface.
    has   NamePattern    std::string    [-]  ?
    has   ExtentTexels   std::uint32_t  [-]  ?
    has   SpaceIdentity  std::uint32_t  [-]  ?
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  🔴 `50` §5.1: the arrangement is **declared, never conventional**. Packing occlusion, roughness and

T EmissionSpecification                  | AssetInterchange.h | 103-118 | owning                        | -  | What an export produces — which channels, at what extent, in what layout, for which consumer. tiles with, and is **not** a readback of resident tiles. Residency is a display decision bounded by device memory; an export bounded by what happened to be resident is an export whose content depends on where the artist last looked. validation and its naming — the three parts that are wrong most often and cost nothing to check.
    has   Images       std::vector<EmittedImage>  [-]  ?
    has   NamePattern  std::string                [-]  ?
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  🔴 `50` §5: an emission resolves the domain at the declared extent through the same path `20` promotes
    note  🚧 The resolution itself waits on `56`, `70` and `20`. What is built here is the declaration, its

F EmissionSpecification::Validate        | AssetInterchange.h | 117     | api,nonthrowing               | 🚩 | Whether the specification describes an export that can be produced at all. component, a colour-carrying channel in an image declaring no space, and a channel occupying two components anywhere in the specification it loaded second. Refused here rather than discovered by whoever ships the asset.
    in    Materials  const MaterialIndex&  [-]  the declared materials, so a channel no material declares is refused
    out   -          Outcome               [-]  refuses with ContentUnsupported for an extent of zero, an image with no occupied
    by    Api/AtmosphereIntegrator.h, Api/IlluminantPopulation.h, Api/PropertySpecification.h, Api/TilingSpecification.h, Source/AssetInterchange.cpp, Source/AtmosphereIntegrator.cpp, (+3 more)
    note  🔴 A channel emitted twice is two answers to one question, and the consumer reads whichever image

F ResolveName                            | AssetInterchange.h | 131     | api,nonthrowing               | 🚩 | Resolves an emission's declared naming pattern. collides with every other name that lost the same one, and the export overwrites itself.
    in    Pattern       const std::string&  [-]   carrying `{Occupant}`, `{Material}`, `{Channel}` and `{Extent}`
    in    OccupantName  const std::string&  [-]   what the artist called the occupant
    in    MaterialName  const std::string&  [-]   what they called the material
    in    ChannelName   const std::string&  [-]   the channel's own spelling
    in    ExtentTexels  std::uint32_t       [px]  the emitted extent
    out   -             Resolved            [-]   the pattern with every declared substitution applied
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  📝 An unrecognised substitution is left verbatim rather than emptied. A name that silently lost a field

//------------------------------------------------------------------------------------------------------------------------
//                                                    THE INTERCHANGE
//------------------------------------------------------------------------------------------------------------------------

T AssetInterchange                       | AssetInterchange.h | 147-219 | owning                        | -  | Intake and emission as one contract, in both directions. occupants — this document's. ③ derives companions — `38`'s, through `34`, and where the cost lives. occupant the artist will paint on and export.
    has   AssumedUnitScale     static constexpr double   [-]  ?
    has   Declared             EmissionSpecification     [-]  ?
    has   UnsupportedNamed     std::vector<std::string>  [-]  ?
    has   UnsupportedReported  std::uint32_t             [-]  ?
    has   TopologyCount        std::uint32_t             [-]  ?
    has   ImageCount           std::uint32_t             [-]  ?
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  🔴 `50` §2: intake is three steps and they are separate. ① decodes faithfully — the codec's. ② enrols
    note  🔴 `50` §8: a partially failed intake **enrols nothing**. Half a topology enrolled as an occupant is an

F AssetInterchange::IntakeTopology       | AssetInterchange.h | 170     | api,nonthrowing               | 🔴 | Enrols one decoded topology into a topology structure, sealing it. §3 gives neither a default — and carries the structure's own refusal otherwise A scene where each occupant carries its own unit convention is a scene where `02` §3.2's rebasing is correct and the geometry still does not line up. `50` §6: a construct that will not survive is named at the moment it arrives, not at the moment it is missed — by which time the artist has already built on it.
    in    Decoded   const DecodedTopology&  [-]  the decoded specification, faithful to the source
    in    Into      TopologyStructure&      [-]  the structure to enrol into; untouched when the intake refuses
    in    Recorded  IntakeIndex&            [-]  where the assumption, if any, is recorded
    out   -         Outcome                 [-]  refuses with ContentUnsupported for absent positions or absent face indexing — `50`
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  🔴 Unit scale is applied **once, at intake**, and is never carried as a per-occupant multiplier.
    note  🔴 Every construct named in `UnsupportedNamed` is recorded **at intake** rather than at export.

F AssetInterchange::IntakeImage          | AssetInterchange.h | 182     | api,nonthrowing               | 🚩 | Records one decoded image's declared space, or the assumption made in its absence. the working space, and reads the channel measure from `42` at the point of use. There is no inference here from file name, channel count or encoding — and this is the document where the temptation lives, because a file called `_normal` looks like a helpful signal.
    in    Decoded   const DecodedImage&  [-]  the decoded image, its original retained
    in    Recorded  IntakeIndex&         [-]  where the assumption, if any, is recorded
    out   -         Outcome              [-]  refuses with ContentUnsupported for an image of no extent or no component
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  🔴 `50` §4: this document **declares** and does not convert. `36` §3 converts once, at intake, into

F AssetInterchange::DeclareEmission      | AssetInterchange.h | 192     | api,nonthrowing               | 🚩 | Whether an emission may be started against the current document. wrong arrangement and the doubled channel are caught — before the artist has waited for an export.
    in    Declaring  const EmissionSpecification&  [-]  the emission specification
    in    Materials  const MaterialIndex&          [-]  the declared materials
    out   -          Outcome                       [-]  carries the specification's own refusal
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp
    note  🚧 `50` §5's resolution waits on `56`, `70` and `20`. Validation does not, and it is where the

F AssetInterchange::Emission             | AssetInterchange.h | 197     | api,nonallocating,nonthrowing | ✔️ | The declared emission, for whoever presents it.
    out   -  const EmissionSpecification&  [-]  ?
    by    Api/IlluminantPopulation.h, Api/MaterialSpecification.h, Source/AssetInterchange.cpp, Source/ConsoleHost.cpp, Source/IlluminantPopulation.cpp, Source/MaterialSpecification.cpp

F AssetInterchange::Unsupported          | AssetInterchange.h | 202     | api,nonallocating,nonthrowing | ✔️ | Constructs that were named at intake and will not survive an emission — `86`'s `50` §6 row.
    out   -  const std::vector<std::string>&  [-]  ?
    by    Source/AssetInterchange.cpp, Source/ConsoleHost.cpp

F AssetInterchange::Report               | AssetInterchange.h | 207     | api,nonthrowing               | 🚩 | Appends the unsupported constructs to the register, once each.
    in    Reporting  ReportSequence&  [-]  ?
    in    Sampled    TickPoint        [-]  ?
    out   -          void             [-]  ?
    by    Api/BrushSpecification.h, Api/ChartPartition.h, Api/IntakeIndex.h, Source/AssetInterchange.cpp, Source/BrushSpecification.cpp, Source/ChartPartition.cpp, (+3 more)

F AssetInterchange::IntakenTopologyCount | AssetInterchange.h | 209     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Source/AssetInterchange.cpp

F AssetInterchange::IntakenImageCount    | AssetInterchange.h | 210     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Source/AssetInterchange.cpp

F SLATE_DECLARES_PRECISION               | AssetInterchange.h | 223     | -                             | -  | ?
    in    Bounded  PrecisionGuarantee::  [-]  ?
    in    Bounded  PrecisionGuarantee::  [-]  ?
    in    Exact    PrecisionGuarantee::  [-]  ?
    by    Api/AtmosphereIntegrator.h, Api/BrushSpecification.h, Api/CameraProjection.h, Api/ChartPartition.h, Api/ColourProjection.h, Api/CurveSolver.h, (+13 more)
