//============================================================================================================================================
//                                                              API.SYMBOLINDEX
//============================================================================================================================================
// 🧩 Everything the application holds that is not the document and not a panel's own layout — with exactly one owner.

%format     symbolindex 1.0
%scope      folder
%path       Engine/SlateDocument/Document/ToolSequence/Api
%layer      SlateDocument
%sources    1
%symbols    37
%annotated  24/37
%cost       ✔️ low · 🚩 medium · 🔴 high (cost rises left to right)

//------------------------------------------------------------------------------------------------------------------------
//                                                        SOURCES
//------------------------------------------------------------------------------------------------------------------------

S ToolSequence.h | 306 lines | b4a824aa | 37 sym | Everything the application holds that is not the document and not a panel's own layout — with exactly one owner.

//------------------------------------------------------------------------------------------------------------------------
//                                                        SYMBOLS
//------------------------------------------------------------------------------------------------------------------------

V AbsentTool                       | ToolSequence.h | 24      | -                             | -  | ?
    by    Source/ToolSequence.cpp

//------------------------------------------------------------------------------------------------------------------------
//                                                   POINTER PRECEDENCE
//------------------------------------------------------------------------------------------------------------------------

E PointerPrecedence                | ToolSequence.h | 34-41   | contract                      | -  | `14` §4.2's four levels, in the order they are consulted. a table. Nothing outside this enumeration may hold the pointer.
    has   Interface        PointerPrecedence  [-]  ?
    has   Manipulator      PointerPrecedence  [-]  ?
    has   Stroke           PointerPrecedence  [-]  ?
    has   Workspace        PointerPrecedence  [-]  ?
    has   PrecedenceCount  PointerPrecedence  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp
    note  🔴 The ordinal **is** the precedence, ascending, so an arbitration is one integer comparison rather than

//------------------------------------------------------------------------------------------------------------------------
//                                                   WHAT IS PRESENTED
//------------------------------------------------------------------------------------------------------------------------

E OverlaySubject                   | ToolSequence.h | 49-59   | contract                      | -  | Which of `80` §3's overlays is presented.
    has   GroundLattice      OverlaySubject  [-]  ?
    has   Guide              OverlaySubject  [-]  ?
    has   Wireframe          OverlaySubject  [-]  ?
    has   SeamDisplay        OverlaySubject  [-]  ?
    has   SurfaceAnnotation  OverlaySubject  [-]  ?
    has   Manipulator        OverlaySubject  [-]  ?
    has   Pivot              OverlaySubject  [-]  ?
    has   OverlayCount       OverlaySubject  [-]  ?
    by    Api/OverlayProjection.h, Source/ConsoleHost.cpp, Source/OverlayProjection.cpp, Source/ToolSequence.cpp

E DisplaySubject                   | ToolSequence.h | 66-72   | contract                      | -  | How the workspace presents the shaded result. because a document that reopened in someone else's display mode has restored a decision about the machine rather than about the work.
    has   Shaded           DisplaySubject  [-]  ?
    has   ChannelIsolated  DisplaySubject  [-]  ?
    has   Unlit            DisplaySubject  [-]  ?
    has   DisplayCount     DisplaySubject  [-]  ?
    by    Source/ToolSequence.cpp
    note  ⚠️ Read by `66` and by `14`, and stored in neither. `76` §2: none of this travels with the document,

E PreviewSubject                   | ToolSequence.h | 76-83   | contract                      | -  | What `82` presents for a tool before the artist commits.
    has   Absent        PreviewSubject  [-]  ?
    has   Impression    PreviewSubject  [-]  ?
    has   Placement     PreviewSubject  [-]  ?
    has   Parameter     PreviewSubject  [-]  ?
    has   PreviewCount  PreviewSubject  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

E TransactionSubject               | ToolSequence.h | 90-96   | contract                      | -  | What shape a tool's edit takes in `10` §2.4's lifecycle. edit is a drag seal a transaction at different moments, and inferring it from whether the pointer moved would make a tap and a one-pixel drag two different operations.
    has   Unrecorded    TransactionSubject  [-]  ?
    has   Immediate     TransactionSubject  [-]  ?
    has   Dragged       TransactionSubject  [-]  ?
    has   SubjectCount  TransactionSubject  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp
    note  🔴 Declared per tool rather than inferred from the pointer. A tool whose edit is immediate and one whose

//------------------------------------------------------------------------------------------------------------------------
//                                                        ONE TOOL
//------------------------------------------------------------------------------------------------------------------------

T ToolSpecification                | ToolSequence.h | 107-115 | owning                        | -  | One tool — its parameters, and the three behaviours a caller must know without knowing which tool it is. `ToolPanel` presents any tool without knowing which. A tool that presents itself through hand-written panel code is a tool the panel has to be edited to add, and the panel then knows every tool by name.
    has   Identity    std::string         [-]  ?
    has   Presented   std::string         [-]  ?
    has   Parameters  PropertyIndex       [-]  ?
    has   Claimed     PointerPrecedence   [-]  ?
    has   Previewed   PreviewSubject      [-]  ?
    has   Recorded    TransactionSubject  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp
    note  🔴 Parameters are `PropertySpecification` declarations from `10` §2.2 — typed, named, validated — so

T ToolIndex                        | ToolSequence.h | 119-146 | owning                        | -  | Every declared tool, addressed by the ordinal its declaration returned.
    has   ToolCeiling  static constexpr std::uint32_t  [-]  ?
    has   Declared     std::vector<ToolSpecification>  [-]  ?
    by    Source/ToolSequence.cpp

F ToolIndex::Declare               | ToolSequence.h | 128     | api,nonthrowing               | 🚩 | Declares one tool and issues its ordinal. and with ExtentExhausted at the declared ceiling
    in    Declaring  const ToolSpecification&  [-]  ?
    out   -          Outcome                   [-]  refuses with ContentUnsupported for an empty identity or an out-of-range declaration,
    by    Api/AttachmentIndex.h, Api/BrushSpecification.h, Api/CameraProjection.h, Api/DecalProjection.h, Api/DescriptorIndex.h, Api/DiagnosticExtension.h, (+65 more)

F ToolIndex::Resolve               | ToolSequence.h | 130     | -                             | -  | ?
    in    ToolOrdinal  std::uint32_t                      [-]  ?
    out   -            Outcome<const ToolSpecification*>  [-]  ?
    by    Api/AtmosphereIntegrator.h, Api/AttachmentIndex.h, Api/BrushSpecification.h, Api/DecalProjection.h, Api/DescriptorIndex.h, Api/DocumentSession.h, (+97 more)

F ToolIndex::Amend                 | ToolSequence.h | 131     | -                             | -  | ?
    in    ToolOrdinal  std::uint32_t                [-]  ?
    out   -            Outcome<ToolSpecification*>  [-]  ?
    by    Api/BrushSpecification.h, Api/CameraProjection.h, Api/DecalProjection.h, Api/DescriptorIndex.h, Api/IlluminantPopulation.h, Api/ImpressionSequence.h, (+26 more)

F ToolIndex::Located               | ToolSequence.h | 137     | api,nonthrowing               | 🚩 | The ordinal one identity was declared at.
    in    Identity  const std::string&  [-]  ?
    out   -         Outcome             [-]  refuses with ContentUnsupported when nothing declares that identity
    by    Api/DocumentSession.h, Api/IlluminantPopulation.h, Api/OcclusionProjection.h, Api/PointerIntersection.h, Api/PropertySpecification.h, Api/ReportSequence.h, (+26 more)

F ToolIndex::DeclaredCount         | ToolSequence.h | 139     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Api/AttachmentIndex.h, Api/BrushSpecification.h, Api/DecalProjection.h, Api/DescriptorIndex.h, Api/GlyphDepot.h, Api/HardwareMetrics.h, (+36 more)

//------------------------------------------------------------------------------------------------------------------------
//                                                  THE POINTER CAPTURE
//------------------------------------------------------------------------------------------------------------------------

T PointerCapture                   | ToolSequence.h | 158-163 | nonallocating,nonthrowing     | -  | Who holds the pointer, and what the capture was taken against. all. `78` §2 fixes the drag plane at Open and never re-derives it per sample; re-deriving it makes the manipulated object chase the cursor with increasing gain, which reads as the manipulator being slippery rather than as the plane being wrong.
    has   Holder           PointerPrecedence  [-]  ?
    has   Opened           ResolvedPointer    [-]  ?
    has   CaptureDeclared  bool               [-]  ?
    by    Source/ToolSequence.cpp
    note  🔴 `Opened` is the pick as it stood at the moment of capture, and it is the reason `76` reads `74` at

//------------------------------------------------------------------------------------------------------------------------
//                                                   THE TOOL SEQUENCE
//------------------------------------------------------------------------------------------------------------------------

T ToolSequence                     | ToolSequence.h | 181-304 | owning                        | -  | Every item of state `14` §4.1 places beside the document, with one owner and one copy. step back through a colour change: the artist who picks a colour, paints, and undoes expects the stroke to disappear and the colour to remain. cannot link `SlateUI` — `00` §2. This is `86` §3's reasoning about the register, one layer up: the storage lives where both can reach it and the interface presents it rather than owning it. Held in `SlateUI` instead, `22` could not read the active colour, and the stroke would resolve against something else — which is the defect the artist meets in the first minute. is written into the file, and none of this is: `76` §2 keeps every row here per application.
    has   DeclaredTools     ToolIndex            [-]  ?
    has   DeclaredBrushes   BrushIndex           [-]  ?
    has   ActiveColour      ColourSpecification  [-]  ?
    has   StandingCapture   PointerCapture       [-]  ?
    has   PresentedDisplay  DisplaySubject       [-]  ?
    has   PresentedChannel  ChannelSubject       [-]  ?
    has   ToolOrdinal       std::uint32_t        [-]  ?
    has   BrushOrdinal      std::uint32_t        [-]  ?
    has   OverlayPresent    bool[OverlaySpan]    [-]  ?
    by    Api/OverlayProjection.h, Source/ConsoleHost.cpp, Source/OverlayProjection.cpp, Source/ToolSequence.cpp
    note  🔴 Nothing here is a transaction and nothing here enters `RevisionSequence` — `14` §4.1. Undo must not
    note  🔴 Held in `SlateDocument.lib` because **both** `SlateCompute` and `SlateUI` read it and `SlateCompute`
    note  ⚠️ Living in `SlateDocument.lib` is a **link-unit** fact and not a persistence one. `48` §2 decides what

F ToolSequence::Tools              | ToolSequence.h | 188     | api,nonallocating,nonthrowing | ✔️ | The declared tools, for presentation and for activation.
    out   -  ToolIndex&  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::Tools              | ToolSequence.h | 189     | -                             | -  | ?
    out   -  const ToolIndex&  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::Brushes            | ToolSequence.h | 197     | api,nonallocating,nonthrowing | ✔️ | The application's brush declarations — `58` §7's per-application store. declared home for per-application state. `48` §6's rule places both on the application side, so a second home would be a second lifetime for one collection.
    out   -  BrushIndex&  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp
    note  📝 Held here rather than beside `58` because `58` declares what a brush **is** and `76` is the

F ToolSequence::Brushes            | ToolSequence.h | 198     | -                             | -  | ?
    out   -  const BrushIndex&  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::DeclareTool        | ToolSequence.h | 204     | api,nonthrowing               | ✔️ | Activates one declared tool.
    in    ToolOrdinal  std::uint32_t  [-]  ?
    out   -            Outcome        [-]  refuses with ContentUnsupported outside the declared tool count
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::DeclareBrush       | ToolSequence.h | 210     | api,nonthrowing               | ✔️ | Activates one declared brush.
    in    BrushOrdinal  std::uint32_t  [-]  ?
    out   -             Outcome        [-]  refuses with ContentUnsupported outside the declared brush count
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::DeclareColour      | ToolSequence.h | 221     | api,nonthrowing               | ✔️ | Declares the active colour. interpret differently, and all three will look plausible. arrives here is therefore in the working space, and a display-referred sample is a separate action that says so.
    in    Declaring  const ColourSpecification&  [-]  ?
    out   -          Outcome                     [-]  refuses with ContentUnsupported for a colour declaring no space
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp
    note  🔴 `36` §1: no bare triple. A colour without its space is a number three subsystems will each
    note  ⚠️ `36` §6 rules a colour sampled from the workspace **scene-referred**, sampled before `66`. What

F ToolSequence::DeclareDisplay     | ToolSequence.h | 227     | api,nonthrowing               | ✔️ | Declares how the workspace presents the shaded result.
    in    Declaring  DisplaySubject  [-]  ?
    out   -          Outcome         [-]  refuses with ContentUnsupported for a mode outside the declared set
    by    Source/ToolSequence.cpp

F ToolSequence::DeclareChannel     | ToolSequence.h | 233     | api,nonthrowing               | ✔️ | Declares which of `42`'s channels is presented alone at `ChannelIsolated`.
    in    Declaring  ChannelSubject  [-]  ?
    out   -          Outcome         [-]  refuses with ContentUnsupported for a channel outside `42`'s twenty
    by    Api/BrushSpecification.h, Api/MaterialSpecification.h, Source/BrushSpecification.cpp, Source/ChannelPanel.cpp, Source/ConsoleHost.cpp, Source/MaterialSpecification.cpp, (+1 more)

F ToolSequence::DeclareOverlay     | ToolSequence.h | 239     | api,nonthrowing               | ✔️ | Declares whether one of `80`'s overlays is presented.
    in    Declaring        OverlaySubject  [-]  ?
    in    PresenceEnabled  bool            [-]  ?
    out   -                Outcome         [-]  refuses with ContentUnsupported outside the declared set
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::OpenCapture        | ToolSequence.h | 250     | api,nonthrowing               | ✔️ | Takes the pointer at a declared precedence, recording the pick it was taken against. persists for the whole drag, and re-arbitrating per sample is the defect where a stroke stops the moment the cursor crosses a floating panel. Arbitration happens before capture is taken, once.
    in    Claiming  PointerPrecedence       [-]  which level is claiming it
    in    Opened    const ResolvedPointer&  [-]  the pick as `74` resolved it at this instant
    out   -         Outcome                 [-]  refuses with HostDenied while a capture already stands
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp
    note  🔴 A standing capture is **not** stolen by a stronger claimant. `76` §3 and `14` §4.2: capture

F ToolSequence::ReleaseCapture     | ToolSequence.h | 257     | api,nonthrowing               | ✔️ | Releases the standing capture.
    out   -  Outcome  [-]  refuses with HostDenied when no capture stands
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp
    note  🔴 Releasing is an explicit event and never a consequence of the pointer moving elsewhere.

F ToolSequence::Arbitrate          | ToolSequence.h | 268     | api,nonallocating,nonthrowing | ✔️ | Which precedence would take the pointer, given what is open and where the pointer is. `SlateUI`. The interface supplies its own condition as an argument.
    in    InterfaceReported  bool      [-]  the interface reports the pointer over itself — `14` §4.2's level 1
    in    ManipulatorOpen    bool      [-]  a drag stands in `78`
    in    StrokeOpen         bool      [-]  a stroke stands in `22`
    out   -                  Claiming  [-]  the standing holder while one stands, the arbitration otherwise
    by    Api/WorkspaceSequence.h, Source/ConsoleHost.cpp, Source/ToolSequence.cpp, Source/WorkspaceSequence.cpp
    note  📝 Declared here rather than in `14` because `22` and `78` both ask it and neither may read

F ToolSequence::Colour             | ToolSequence.h | 270     | -                             | -  | ?
    out   -  const ColourSpecification&  [-]  ?
    by    Api/ControlPanel.h, Api/PropertySpecification.h, Api/ThemeSpecification.h, Source/AttachmentIndex.cpp, Source/ConsoleHost.cpp, Source/ControlChrome.cpp, (+9 more)

F ToolSequence::Capture            | ToolSequence.h | 271     | -                             | -  | ?
    out   -  const PointerCapture&  [-]  ?
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::ActiveTool         | ToolSequence.h | 277     | api,nonthrowing               | ✔️ | The active tool, refusing where none has been activated.
    out   -  Outcome  [-]  refuses with ContentUnsupported before a tool is declared active
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::ActiveBrush        | ToolSequence.h | 283     | api,nonthrowing               | ✔️ | The active brush, refusing where none has been activated.
    out   -  Outcome  [-]  refuses with ContentUnsupported before a brush is declared active
    by    Source/ConsoleHost.cpp, Source/ToolSequence.cpp

F ToolSequence::ActiveToolOrdinal  | ToolSequence.h | 285     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Source/ToolSequence.cpp

F ToolSequence::ActiveBrushOrdinal | ToolSequence.h | 286     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Source/ToolSequence.cpp

F ToolSequence::Display            | ToolSequence.h | 287     | -                             | -  | ?
    out   -  DisplaySubject  [-]  ?
    by    Api/DisplayProjection.h, Api/WorkspaceSequence.h, Source/ConsoleHost.cpp, Source/DisplayProjection.cpp, Source/ToolSequence.cpp, Source/WorkspaceSequence.cpp

F ToolSequence::IsolatedChannel    | ToolSequence.h | 288     | -                             | -  | ?
    out   -  ChannelSubject  [-]  ?
    by    Source/ToolSequence.cpp

F ToolSequence::OverlayStanding    | ToolSequence.h | 289     | -                             | -  | ?
    in    Subject  OverlaySubject  [-]  ?
    out   -        bool            [-]  ?
    by    Api/OverlayProjection.h, Source/ConsoleHost.cpp, Source/OverlayProjection.cpp, Source/ToolSequence.cpp
