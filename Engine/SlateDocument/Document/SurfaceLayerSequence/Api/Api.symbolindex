//============================================================================================================================================
//                                                              API.SYMBOLINDEX
//============================================================================================================================================
// 🧩 The ordered content of one surface — the single source of truth a resident tile is a projection of.

%format     symbolindex 1.0
%scope      folder
%path       Engine/SlateDocument/Document/SurfaceLayerSequence/Api
%layer      SlateDocument
%sources    1
%symbols    32
%annotated  28/32
%cost       ✔️ low · 🚩 medium · 🔴 high (cost rises left to right)

//------------------------------------------------------------------------------------------------------------------------
//                                                        SOURCES
//------------------------------------------------------------------------------------------------------------------------

S SurfaceLayerSequence.h | 361 lines | 4ff55bc2 | 32 sym | The ordered content of one surface — the single source of truth a resident tile is a projection of.

//------------------------------------------------------------------------------------------------------------------------
//                                                THE FOUR CONTENT SOURCES
//------------------------------------------------------------------------------------------------------------------------

E LayerContentSource                        | SurfaceLayerSequence.h | 31-39   | contract                      | -  | Where an entry's content comes from — `56` §3's four sources. reduction level was promoted, which is what `20` §2.1's third reconstruction source reads.
    has   PaintedImpressions  LayerContentSource  [-]  ?
    has   PlacedContent       LayerContentSource  [-]  ?
    has   Tiling              LayerContentSource  [-]  ?
    has   AnalyticResolution  LayerContentSource  [-]  ?
    has   NestedSequence      LayerContentSource  [-]  ?
    has   SourceCount         LayerContentSource  [-]  ?
    by    Api/LayerPanel.h, Api/SurfaceDepot.h, Source/AnalyticProjection.cpp, Source/ConsoleHost.cpp, Source/LayerPanel.cpp, Source/PromotionScheduler.cpp, (+3 more)
    note  🔴 Only the first stores texels. The other three store a **description** that `70` resolves at whatever

F SourceReconstructible                     | SurfaceLayerSequence.h | 46-49   | api,nonallocating,nonthrowing | ✔️ | Whether content from one source can be rebuilt from what the document holds. content itself would be classifying content it can only see as texels.
    in    Source  LayerContentSource  [-]  ?
    out   -       constexpr bool      [-]  ?
    by    Source/SurfaceDepot.cpp, Source/SurfaceLayerSequence.cpp
    note  🔴 `20` §4 never decides this. It reads the decision made here, because a residency system classifying

//------------------------------------------------------------------------------------------------------------------------
//                                                     PAINTED TEXELS
//------------------------------------------------------------------------------------------------------------------------

T PaintedContent                            | SurfaceLayerSequence.h | 62-67   | owning                        | -  | The one thing in a document that is stored as texels rather than as a description. change of working extent or a domain re-partition resamples. Everything else is re-resolved, and re-resolution is exact where resampling is not. allocations rather than thirty times the channel count.
    has   Texels          std::vector<float>  [-]  ?
    has   ExtentTexels    std::uint32_t       [-]  ?
    has   ComponentCount  std::uint32_t       [-]  ?
    by    Api/ReferenceIndex.h, Source/AnalyticProjection.cpp, Source/ImpressionSequence.cpp, Source/ReferenceIndex.cpp, Source/SurfaceLayerSequence.cpp
    note  🔴 `56` §3: painted texels are stored at the surface's **working extent** and are the one content a
    note  💾 Held interleaved, one span per entry, so that a surface with thirty painted layers is thirty

//------------------------------------------------------------------------------------------------------------------------
//                                                 WHERE A CHANNEL LANDS
//------------------------------------------------------------------------------------------------------------------------

T ChannelPlacement                          | SurfaceLayerSequence.h | 83-88   | nonallocating,nonthrowing     | -  | Which components of one painted entry a channel occupies. not the only reader. `70` resolves into the same components and sits below `22` in `00` §9.1's strata, so a declaration held there would be a back edge from stratum 7 to stratum 9. open, so deriving one here would answer that question in the one place nobody would look for it. inferred from the measure, so a two-component packing of a colour refuses at the caller's validation instead of writing three channels over each other.
    has   Channel           ChannelSubject  [-]  ?
    has   ComponentOrdinal  std::uint32_t   [-]  ?
    has   ComponentSpan     std::uint32_t   [-]  ?
    by    Api/AnalyticProjection.h, Api/EmissionSequence.h, Api/ImpressionSequence.h, Api/PreviewProjection.h, Api/ReflectanceIntegrator.h, Source/AnalyticProjection.cpp, (+5 more)
    note  🔴 Declared here rather than in `22`, because it describes the layout of a `PaintedContent` and `22` is
    note  🚧 Supplied by the caller rather than derived from `42`. `00` §12 carries the channel packing layout as
    note  🔴 A colour channel spans three components and a scalar one spans one. The span is declared rather than

//------------------------------------------------------------------------------------------------------------------------
//                                                        COVERAGE
//------------------------------------------------------------------------------------------------------------------------

T CoverageSpecification                     | SurfaceLayerSequence.h | 101-108 | owning                        | -  | Where an entry applies, and how strongly. Painting coverage is a stroke, undone by `22` §4's extent-bounded inverse, and nothing about it is a separate mechanism. analytically. A coverage that could only be painted would make a tiled mask impossible.
    has   Source            LayerContentSource  [-]  ?
    has   SourceOrdinal     std::uint32_t       [-]  ?
    has   Painted           PaintedContent      [-]  ?
    has   UniformStrength   double              [-]  ?
    has   CoverageDeclared  bool                [-]  ?
    note  🔴 `56` §5: coverage is **content**, and is revised through `RevisionSequence` like any other content.
    note  📝 It carries the same four sources §3 declares, because it may be painted, placed, tiled or resolved

//------------------------------------------------------------------------------------------------------------------------
//                                                       ONE ENTRY
//------------------------------------------------------------------------------------------------------------------------

T LayerSpecification                        | SurfaceLayerSequence.h | 124-136 | owning                        | -  | One entry of a surface's content, declaring all four of `56` §2's fields and nothing implicit. between impressions within a stroke; this applies it between entries. Two documents declaring two sets of behaviours produce a surface whose result changes depending on whether content arrived as a stroke or as a layer, which the artist cannot see and cannot correct. an artist may legitimately hold a layer that writes nothing while they decide; what is forbidden is defaulting to all twenty, which silently overwrites roughness with a colour layer and is discovered at export.
    has   Identity         LayerIdentity          [-]  ?
    has   Source           LayerContentSource     [-]  ?
    has   SourceOrdinal    std::uint32_t          [-]  ?
    has   NestedOrdinal    std::uint32_t          [-]  ?
    has   ChannelMask      std::uint32_t          [-]  ?
    has   Combination      CombineSpecification   [-]  ?
    has   Coverage         CoverageSpecification  [-]  ?
    has   Painted          PaintedContent         [-]  ?
    has   PresenceEnabled  bool                   [-]  ?
    has   ResampleOwed     bool                   [-]  ?
    by    Api/AnalyticProjection.h, Source/AnalyticProjection.cpp, Source/ConsoleHost.cpp, Source/LayerPanel.cpp, Source/PromotionScheduler.cpp, Source/SurfaceLayerSequence.cpp
    note  🔴 The combination is `22` §3's `CombineSpecification`, unamended and not re-declared. `22` applies it
    note  ⚠️ An entry declaring no channel subset writes **nothing**. It is admitted rather than refused, because

F EntryWritesChannel                        | SurfaceLayerSequence.h | 141-145 | api,nonallocating,nonthrowing | ✔️ | Whether an entry writes one of `42`'s channels.
    in    Entry    const LayerSpecification&  [-]  ?
    in    Channel  ChannelSubject             [-]  ?
    out   -        constexpr bool             [-]  ?

//------------------------------------------------------------------------------------------------------------------------
//                                                      THE SEQUENCE
//------------------------------------------------------------------------------------------------------------------------

T SurfaceLayerSequence                      | SurfaceLayerSequence.h | 162-329 | owning                        | -  | The ordered content of one surface, per channel, and the only ordering authority over it. is a sequence position. priority or an ordinal that could disagree with its position, and `12`'s outliner **presents** that order rather than holding a second one — which is why reordering in the outliner and reordering in a layer panel can never disagree. `68`'s parametric domain, and `20` projects the sequence onto whatever is resident. A sequence holding device extents would make the working extent a property of the file, and changing it an import.
    has   EntryCeiling         static constexpr std::uint32_t     [-]  ?
    has   LayerNestingCeiling  static constexpr std::uint32_t     [-]  ?
    has   Sequenced            std::vector<LayerSpecification>    [-]  ?
    has   NestedSequences      std::vector<SurfaceLayerSequence>  [-]  ?
    has   IssuedGeneration     std::uint32_t                      [-]  ?
    has   Depth                std::uint32_t                      [-]  ?
    has   DescribedRevision    std::uint64_t                      [-]  ?
    by    Api/AnalyticProjection.h, Api/EmissionSequence.h, Api/ImpressionSequence.h, Api/LayerPanel.h, Api/PreviewProjection.h, Api/PromotionScheduler.h, (+10 more)
    note  ⚠️ `Stack` is banned. The ordered content of a surface is a `SurfaceLayerSequence` and a position in it
    note  🔴 `56` §4: sequence position is the sole answer to "what is on top". No entry carries a depth, a
    note  🔴 `56` §7: nothing here addresses a tile, a texel population or a device resource. Entries address

F SurfaceLayerSequence::Append              | SurfaceLayerSequence.h | 174     | api,nonthrowing               | 🚩 | Appends one entry at the end of the sequence, issuing its identity. painted content whose span does not match its declared extent, and with ExtentExhausted at the entry ceiling
    in    Declaring  const LayerSpecification&  [-]  every one of §2's four fields
    out   -          Outcome                    [-]  refuses with ContentUnsupported for a nested entry beyond the declared depth, for
    post  the entry sits last, which is topmost
    by    Api/FileInterchange.h, Api/RecoverySequence.h, Api/ReportSequence.h, Source/AssetInterchange.cpp, Source/BrushSpecification.cpp, Source/ChartPartition.cpp, (+13 more)

F SurfaceLayerSequence::Reorder             | SurfaceLayerSequence.h | 185     | api,nonthrowing               | 🚩 | Moves one entry to a declared sequence position. position so the caller's inverse is one ordinal rather than a whole ordering nothing else, which is what makes reordering affordable in `RevisionSequence`.
    in    Subject   LayerIdentity  [-]  the entry
    in    Position  std::uint32_t  [-]  where it should sit; clamped to the end
    out   -         Outcome        [-]  refuses with IdentityStale when the entry no longer resolves, and carries the prior
    by    Api/WorkspaceSpace.h, Source/LayerPanel.cpp, Source/SurfaceLayerSequence.cpp, Source/WorkspaceDrag.cpp, Source/WorkspaceStrip.cpp
    note  🔴 One transaction against two sequence positions — `56` §6. The inverse is the prior position and

F SurfaceLayerSequence::DeclarePresence     | SurfaceLayerSequence.h | 193     | api,nonthrowing               | 🚩 | Presents or hides one entry. presented has been told the document does not hold what they see.
    in    Subject          LayerIdentity  [-]  ?
    in    PresenceEnabled  bool           [-]  ?
    out   -                Outcome        [-]  refuses with IdentityStale; carries the prior standing as the inverse
    by    Api/OutlinerPanel.h, Source/LayerPanel.cpp, Source/OutlinerPanel.cpp, Source/SurfaceLayerSequence.cpp
    note  ⚠️ A recorded amendment, per `56` §6. An artist who hides a layer, saves, reopens and finds it

F SurfaceLayerSequence::DeclareCombination  | SurfaceLayerSequence.h | 199     | api,nonthrowing               | 🚩 | Amends one entry's combination.
    in    Subject    LayerIdentity         [-]  ?
    in    Declaring  CombineSpecification  [-]  ?
    out   -          Outcome               [-]  refuses with IdentityStale; carries the prior combination
    by    Api/BrushSpecification.h, Source/BrushSpecification.cpp, Source/SurfaceLayerSequence.cpp

F SurfaceLayerSequence::Withdraw            | SurfaceLayerSequence.h | 209     | api,nonthrowing               | 🚩 | Removes one entry, retaining its description and never its resolved texels. from a description live in `SurfaceDepot` under `20` §4 and are reconstructible by definition, so recording them in an inverse would store a derivation inside the document to undo a change that does not need it.
    in    Subject  LayerIdentity  [-]  ?
    out   -        Outcome        [-]  refuses with IdentityStale
    by    Api/DecalProjection.h, Api/GlyphDepot.h, Api/IlluminantPopulation.h, Api/PointerIntersection.h, Api/PopulationIndex.h, Api/PrimitiveStructure.h, (+16 more)
    note  🔴 `56` §6: removing an entry that stores a description retains the **description**. Texels resolved

F SurfaceLayerSequence::Nest                | SurfaceLayerSequence.h | 221     | api,nonthrowing               | 🚩 | Nests one sequence inside this one as a single entry. ExtentExhausted beyond `LayerNestingCeiling` coverage are each applied **once**, to the nested result. Applying the enclosing coverage per entry instead is the defect that makes a partly covered nested sequence darken at its own internal overlaps — the same defect `22` §3 fixes by accumulating a stroke once and applying it once, at a different scale.
    out   -  Outcome  [-]  the issued ordinal, into this surface's own nested sequences; refuses with
    by    Api/TilingSpecification.h, Source/SurfaceLayerSequence.cpp, Source/TilingSpecification.cpp
    note  🔴 §4.1: the nested content combines **internally first**, and the enclosing entry's combination and

F SurfaceLayerSequence::Resample            | SurfaceLayerSequence.h | 240     | api,nonthrowing               | 🔴 | Resamples every painted entry into a re-partitioned domain, on the tick. position it occupied in the old one unwrap; **this** resamples the authored texels, because `68` is in `SlateCompute` and cannot reach the document content at all. The remapping crosses as a callable so that nothing here names `68`. for exactly that reason. Everything else is re-resolved, and re-resolution is exact where resampling is not. not a defect in it; what would be a defect is performing it without saying so.
    in    ArrivingRevision  std::uint64_t                                                 [-]   the partition revision `68` advanced to
    in    Remapping         const std::function<bool(double, double, double&, double&)>&  [-]   supplied by the caller: a position in the new domain, answered with the
    in    Reporting         ReportSequence&                                               [-]   where `86` §4's `56` §3.1 row lands
    in    Sampled           TickPoint                                                     [ns]  the tick's own reading
    out   -                 Outcome                                                       [-]   refuses with HostDenied when no remapping is supplied
    by    Source/SurfaceLayerSequence.cpp
    note  🔴 `00` §10 conflict 40, discharged from this side. `68` advances the revision and derives the
    note  🔴 This is the **one operation in the engine that resamples authored content**, and it is reported
    note  ⚠️ Sampling is bilinear, so a re-partition softens paint. That is a property of the operation and

F SurfaceLayerSequence::Resolve             | SurfaceLayerSequence.h | 249     | api,nonthrowing               | ✔️ | One entry, by identity.
    in    Subject  LayerIdentity  [-]  ?
    out   -        Outcome        [-]  refuses with IdentityStale
    by    Api/AtmosphereIntegrator.h, Api/AttachmentIndex.h, Api/BrushSpecification.h, Api/DecalProjection.h, Api/DescriptorIndex.h, Api/DocumentSession.h, (+95 more)

F SurfaceLayerSequence::AmendPainted        | SurfaceLayerSequence.h | 262     | api,nonthrowing               | 🚩 | One entry's painted texels, for the one mechanism permitted to amend them. ContentUnsupported when its source is not PaintedImpressions describes — §3 — so this is the one route by which authored content is written at all, and every write through it is inside a transaction whose inverse is bounded by the extents it touched. resolved projection the source of truth for content the description already holds, and `20` §4 would then evict the artist's edit at the first memory pressure.
    in    Subject  LayerIdentity  [-]  ?
    out   -        Outcome        [-]  refuses with IdentityStale when the entry no longer resolves, and with
    by    Source/ConsoleHost.cpp, Source/ImpressionSequence.cpp, Source/SurfaceLayerSequence.cpp
    note  🔴 `22` is the only caller. Painted texels are the one content this sequence stores rather than
    note  ⚠️ A reconstructible entry refuses. Writing texels into a placed or tiled entry would make the

F SurfaceLayerSequence::Entries             | SurfaceLayerSequence.h | 267     | api,nonallocating,nonthrowing | ✔️ | The entries, in sequence order — bottom first.
    out   -  const std::vector<LayerSpecification>&  [-]  ?
    by    Api/RecoverySequence.h, Source/AnalyticProjection.cpp, Source/LayerPanel.cpp, Source/PromotionScheduler.cpp, Source/RecoverySequence.cpp, Source/SpatialSubdivision.cpp, (+1 more)

F SurfaceLayerSequence::Nested              | SurfaceLayerSequence.h | 273     | api,nonthrowing               | ✔️ | One nested sequence, by ordinal.
    in    NestedOrdinal  std::uint32_t  [-]  ?
    out   -              Outcome        [-]  refuses with ContentUnsupported outside the nested count
    by    Source/AnalyticProjection.cpp, Source/SceneStructure.cpp, Source/SurfaceLayerSequence.cpp

F SurfaceLayerSequence::AmendNested         | SurfaceLayerSequence.h | 279     | api,nonthrowing               | ✔️ | One nested sequence, for amending.
    in    NestedOrdinal  std::uint32_t  [-]  ?
    out   -              Outcome        [-]  refuses with ContentUnsupported outside the nested count
    by    Source/SurfaceLayerSequence.cpp

F SurfaceLayerSequence::PositionOf          | SurfaceLayerSequence.h | 285     | api,nonthrowing               | ✔️ | Which sequence position one entry sits at.
    in    Subject  LayerIdentity  [-]  ?
    out   -        Outcome        [-]  refuses with IdentityStale
    by    Source/SurfaceLayerSequence.cpp

F SurfaceLayerSequence::WrittenChannels     | SurfaceLayerSequence.h | 290     | api,nonallocating,nonthrowing | 🚩 | The union of channels every presented entry writes — what `42`'s layered channels resolve against.
    out   -  std::uint32_t  [-]  ?
    by    Source/SurfaceLayerSequence.cpp

F SurfaceLayerSequence::AuthoredContentHeld | SurfaceLayerSequence.h | 297     | api,nonallocating,nonthrowing | 🚩 | Whether any entry stores texels, and is therefore not evictable. holding a projection of one of these entries may be discarded while the entry may not.
    out   -  bool  [-]  ?
    by    Source/SurfaceLayerSequence.cpp
    note  🔴 `20` §5's gate read from this side: no tile is the source of truth for any content, and a tile

F SurfaceLayerSequence::NestingDepth        | SurfaceLayerSequence.h | 302     | api,nonallocating,nonthrowing | ✔️ | How deeply this sequence is nested; zero for a surface's own.
    out   -  std::uint32_t  [-]  ?
    by    Api/AnalyticProjection.h, Api/HardwareMetrics.h, Api/TilingSpecification.h, Source/AnalyticProjection.cpp, Source/HardwareMetrics.cpp, Source/SurfaceLayerSequence.cpp, (+1 more)

F SurfaceLayerSequence::AddressedRevision   | SurfaceLayerSequence.h | 307     | api,nonallocating,nonthrowing | ✔️ | The partition revision the painted content currently addresses.
    out   -  std::uint64_t  [-]  ?
    by    Source/AnalyticProjection.cpp, Source/SurfaceLayerSequence.cpp

F SurfaceLayerSequence::EntryCount          | SurfaceLayerSequence.h | 309     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Api/SpatialSubdivision.h, Source/AnalyticProjection.cpp, Source/LayerPanel.cpp, Source/SpatialSubdivision.cpp, Source/SurfaceLayerSequence.cpp

F SurfaceLayerSequence::NestedCount         | SurfaceLayerSequence.h | 310     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Source/AnalyticProjection.cpp, Source/SurfaceLayerSequence.cpp

F SurfaceLayerSequence::Located             | SurfaceLayerSequence.h | 322     | -                             | -  | ?
    in    Subject  LayerIdentity  [-]  ?
    out   -        std::size_t    [-]  ?
    by    Api/DocumentSession.h, Api/IlluminantPopulation.h, Api/OcclusionProjection.h, Api/PointerIntersection.h, Api/PropertySpecification.h, Api/ReportSequence.h, (+26 more)

//------------------------------------------------------------------------------------------------------------------------
//                                                      THE ENTRIES
//------------------------------------------------------------------------------------------------------------------------

T LayerIndex                                | SurfaceLayerSequence.h | 339-355 | owning                        | -  | Identity and lookup across one surface's sequence, nested sequences included. question a single sequence cannot answer about an entry it does not hold.
    by    Source/SurfaceLayerSequence.cpp
    note  📝 Declared apart from the sequence because §4.1's nesting makes "which sequence holds this entry" a

F LayerIndex::Locate                        | SurfaceLayerSequence.h | 349     | api,nonthrowing               | 🚩 | Locates one entry anywhere in a sequence, including inside its nested sequences.
    in    Sequence  const SurfaceLayerSequence&  [-]  the surface's own sequence
    in    Subject   LayerIdentity                [-]  the entry
    out   -         Outcome                      [-]  refuses with IdentityStale when nothing in the whole nesting holds it
    by    Source/SurfaceLayerSequence.cpp

F LayerIndex::SpannedCount                  | SurfaceLayerSequence.h | 354     | api,nonthrowing               | 🚩 | How many entries the whole nesting holds.
    in    Sequence  const SurfaceLayerSequence&  [-]  ?
    out   -         static std::uint32_t         [-]  ?
    by    Api/DocumentSession.h, Api/IlluminantPopulation.h, Api/OcclusionProjection.h, Api/PopulationIndex.h, Api/PrimitiveStructure.h, Api/SceneStructure.h, (+10 more)

F SLATE_DECLARES_PRECISION                  | SurfaceLayerSequence.h | 359     | -                             | -  | ?
    in    Bounded  PrecisionGuarantee::  [-]  ?
    in    Bounded  PrecisionGuarantee::  [-]  ?
    in    Exact    PrecisionGuarantee::  [-]  ?
    by    Api/AnalyticProjection.h, Api/AssetInterchange.h, Api/AtmosphereIntegrator.h, Api/BrushSpecification.h, Api/CameraProjection.h, Api/ChannelPanel.h, (+51 more)
