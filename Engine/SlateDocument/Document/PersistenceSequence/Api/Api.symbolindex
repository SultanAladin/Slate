//============================================================================================================================================
//                                                              API.SYMBOLINDEX
//============================================================================================================================================
// 🧩 `48` §3 — write, verify, replace: the existing file is never touched until a complete replacement has been read back.

%format     symbolindex 1.0
%scope      folder
%path       Engine/SlateDocument/Document/PersistenceSequence/Api
%layer      SlateDocument
%sources    1
%symbols    9
%annotated  8/9
%cost       ✔️ low · 🚩 medium · 🔴 high (cost rises left to right)

//------------------------------------------------------------------------------------------------------------------------
//                                                        SOURCES
//------------------------------------------------------------------------------------------------------------------------

S PersistenceSequence.h | 128 lines | d37fe882 | 9 sym | `48` §3 — write, verify, replace: the existing file is never touched until a complete replacement has been read back.

//------------------------------------------------------------------------------------------------------------------------
//                                                     THE FOUR STEPS
//------------------------------------------------------------------------------------------------------------------------

E PersistenceStep                      | PersistenceSequence.h | 29-37  | contract                      | -  | Where a save stands, so a refusal names which of `48` §3's four steps declined. save is untouched and nothing is lost; a refusal at ③ means the replacement exists beside the target and can be recovered by hand. Reporting all three as "the save failed" throws that away.
    has   Unbegun    PersistenceStep  [-]  ?
    has   Written    PersistenceStep  [-]  ?
    has   Verified   PersistenceStep  [-]  ?
    has   Replaced   PersistenceStep  [-]  ?
    has   Retired    PersistenceStep  [-]  ?
    has   StepCount  PersistenceStep  [-]  ?
    by    Source/DocumentSession.cpp, Source/PersistenceSequence.cpp
    note  🔴 The step matters to the artist, not just to the reporter. A refusal at ① or ② means their previous

T PersistenceConclusion                | PersistenceSequence.h | 41-47  | nonallocating,nonthrowing     | -  | One completed save, and what it leaves true of the document that requested it.
    has   Reached       PersistenceStep  [-]  ?
    has   SavedThrough  std::uint64_t    [-]  ?
    has   SavedAt       std::uint64_t    [-]  ?
    has   SpannedBytes  std::uint64_t    [-]  ?
    by    Api/DocumentSession.h, Source/DocumentSession.cpp, Source/PersistenceSequence.cpp

//------------------------------------------------------------------------------------------------------------------------
//                                                    THE SEALED STATE
//------------------------------------------------------------------------------------------------------------------------

T SealedContent                        | PersistenceSequence.h | 61-68  | owning                        | -  | Everything a save reads, captured at declaration so the resolution reads nothing that can change. half-finished drag is not a state the artist asked to keep — and a document that reopened mid-drag would present a stroke the artist never let go of. artist keeps painting while it does. A save that read the live document would read it mid-transaction on whichever machine happened to be slow enough, and `48` §3's non-blocking rule would be the cause.
    has   Content        std::vector<std::uint8_t>  [-]  ?
    has   TargetPath     std::string                [-]  ?
    has   SavedThrough   std::uint64_t              [-]  ?
    has   SealedAt       std::uint64_t              [-]  ?
    has   StreamVersion  std::uint32_t              [-]  ?
    by    Api/DocumentSession.h, Source/DocumentSession.cpp, Source/PersistenceSequence.cpp
    note  🔴 `48` §3: saving reads a **sealed** state. `10` §2.4's open transactions are not written, because a
    note  🔴 `34` §2: the whole point of this being a captured copy is that the save runs off the tick and the

//------------------------------------------------------------------------------------------------------------------------
//                                                      THE SEQUENCE
//------------------------------------------------------------------------------------------------------------------------

T PersistenceSequence                  | PersistenceSequence.h | 82-123 | owning                        | -  | The write-verify-replace sequence, run over one sealed state through one file surface. and read back. Writing in place is the mechanism by which a full disk, a lost share or a power loss converts a good file into no file at all — and the artist's previous save is their fallback. seam. This sequence is what states them apart for `86`, and it does that by recording which step it reached rather than by performing them through three separate calls.
    has   Reached        PersistenceStep  [-]  ?
    has   ReplacedTotal  std::uint32_t    [-]  ?
    by    Source/PersistenceSequence.cpp
    note  🔴 `48` §3's first rule: the existing file is not touched until a complete replacement has been written
    note  ⚠️ `FileInterchange::WriteStream` is one routine because the three steps must not be separable at that

F PersistenceSequence::Persist         | PersistenceSequence.h | 97     | api,nonthrowing               | 🔴 | Runs `48` §3's four steps over one sealed state. the file surface's own refusal otherwise `34` §2's immutability rule by construction rather than by discipline. session and this routine runs off it. `Reached` is what tells the caller ③ actually landed.
    in    Sealed  const SealedContent&  [-]  the captured content and where it is to land
    out   -       Outcome               [-]  refuses with ContentUnsupported for an empty target path or empty content, and carries
    post  🔴 on any refusal the existing file is exactly as it was — `48` §3
    by    Source/PersistenceSequence.cpp
    note  🔴 This is what `34` resolves at Interactive. Nothing in it reads the document, so it satisfies
    note  📝 The journal retirement at ④ is the caller's, on the tick, because the journal belongs to the

F PersistenceSequence::VerifyIdentical | PersistenceSequence.h | 107    | api,nonallocating,nonthrowing | 🚩 | Whether two streams are identical, byte for byte — `48` §3 ② and `48` §8's exact tier. corruptions a failing disk produces, which are the ones this step exists to catch.
    in    Written  const std::vector<std::uint8_t>&  [-]  what the save handed to the file surface
    in    Landed   const std::vector<std::uint8_t>&  [-]  what was read back from it
    out   -        Identical                         [-]  false at the first difference, and for two different extents
    by    Source/PersistenceSequence.cpp
    note  🔴 Exact, never tolerant. A verification that accepted a near match would accept exactly the

F PersistenceSequence::LastReached     | PersistenceSequence.h | 112    | api,nonallocating,nonthrowing | ✔️ | How far the last save got, whether it delivered or refused.
    out   -  PersistenceStep  [-]  ?
    by    Source/PersistenceSequence.cpp

F PersistenceSequence::ReplacedCount   | PersistenceSequence.h | 117    | api,nonallocating,nonthrowing | ✔️ | How many saves this sequence has completed through ③.
    out   -  std::uint32_t  [-]  ?
    by    Source/PersistenceSequence.cpp

F SLATE_DECLARES_PRECISION             | PersistenceSequence.h | 125    | -                             | -  | ?
    in    Exact  PrecisionGuarantee::  [-]  ?
    in    Exact  PrecisionGuarantee::  [-]  ?
    by    Api/AnalyticProjection.h, Api/AssetInterchange.h, Api/AtmosphereIntegrator.h, Api/BrushSpecification.h, Api/CameraProjection.h, Api/ChannelPanel.h, (+53 more)
