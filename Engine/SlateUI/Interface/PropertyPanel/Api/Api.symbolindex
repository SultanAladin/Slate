//============================================================================================================================================
//                                                              API.SYMBOLINDEX
//============================================================================================================================================
// 🧩 Any declared property presented without the panel knowing which — one row per declaration, read from `PropertyIndex`.

%format     symbolindex 1.0
%scope      folder
%path       Engine/SlateUI/Interface/PropertyPanel/Api
%layer      SlateUI
%sources    1
%symbols    8
%annotated  5/8
%cost       ✔️ low · 🚩 medium · 🔴 high (cost rises left to right)

//------------------------------------------------------------------------------------------------------------------------
//                                                        SOURCES
//------------------------------------------------------------------------------------------------------------------------

S PropertyPanel.h | 114 lines | 81da7cb5 | 8 sym | Any declared property presented without the panel knowing which — one row per declaration, read from `PropertyIndex`.

//------------------------------------------------------------------------------------------------------------------------
//                                                 WHAT THE PANEL CARRIES
//------------------------------------------------------------------------------------------------------------------------

V PropertyRowCapacity      | PropertyPanel.h | 25    | -                                  | -  | ?
    by    Source/PropertyPanel.cpp

V PropertyNoticeExtent     | PropertyPanel.h | 26    | -                                  | -  | ?
    by    Source/PropertyPanel.cpp

T PropertyEditReport       | PropertyPanel.h | 34-40 | contract,nonallocating,nonthrowing | -  | One property's edit lifecycle, reported to whoever owns the transaction. it is a presentation — so it reports the three points and the caller brackets the run. A panel that opened its own would be a second authority on what an edit is, and `76`'s tool parameters would then be recorded twice.
    has   EditOpened    bool           [-]  ?
    has   EditSealed    bool           [-]  ?
    has   ValueWritten  bool           [-]  ?
    has   RowOrdinal    std::uint32_t  [-]  ?
    note  🔴 `10` §2.4: a drag is **one** transaction and not one per tick. This panel does not open transactions —

T PropertyPanelCarry       | PropertyPanel.h | 51-61 | owning                             | -  | What the panel carries between ticks — presentation only, and the caller owns all of it. open dropdowns are layout, and layout inside the document would make opening a dropdown an undoable edit. re-declaration names a different property than it did. Presentation only — a stale carry presents the wrong caret position for one tick and writes nothing, because a write is keyed by identity and not by ordinal.
    has   VisibleOffset   float                               [-]  ?
    has   TextCarries     TextCarry[PropertyRowCapacity]      [-]  ?
    has   ChoiceCarries   DropdownCarry[PropertyRowCapacity]  [-]  ?
    has   PickerOpen      bool[PropertyRowCapacity]           [-]  ?
    has   Notice          char[PropertyNoticeExtent]          [-]  ?
    has   NoticeDeclared  bool                                [-]  ?
    has   PresentedTicks  std::uint32_t                       [-]  ?
    has   Reported        PropertyEditReport                  [-]  ?
    by    Api/TexturePaintSpecification.h, Source/PropertyPanel.cpp
    note  🔴 `14` §4.1: nothing here is a value or a declaration. The folds, the offset, the text carries and the
    note  ⚠️ The text and dropdown carries are addressed by **row ordinal**, so a carry that outlives a

T PropertyPanelContext     | PropertyPanel.h | 68-72 | nonallocating,nonthrowing          | -  | What the panel presents against — the declarations it reads and the carry it writes. `Resolve` on the tick it is presented, so a value written by a tool while the panel is open presents immediately rather than on the next click.
    has   Declarations  PropertyIndex*       [-]  ?
    has   Carry         PropertyPanelCarry*  [-]  ?
    by    Api/TexturePaintSpecification.h, Source/PropertyPanel.cpp
    note  🔴 `14` §1's gate: the panel stores neither, and it holds no value of its own. Every reading comes from

//------------------------------------------------------------------------------------------------------------------------
//                                                    WHAT A ROW READS
//------------------------------------------------------------------------------------------------------------------------

F CaptionOfPropertyMeasure | PropertyPanel.h | 83    | api,nonallocating,nonthrowing      | ✔️ | The caption one measure is presented under. unrelated enumerations and one name over both would read as a shared vocabulary they do not have.
    in    Measured  PropertyMeasure  [-]  ?
    out   -         const char*      [-]  ?
    by    Source/PropertyPanel.cpp
    note  ⚠️ Spelled apart from `ChannelPanel`'s measure caption. `PropertyMeasure` and `ChannelMeasure` are

//------------------------------------------------------------------------------------------------------------------------
//                                                    THE PRESENTATION
//------------------------------------------------------------------------------------------------------------------------

F PresentPropertyPanel     | PropertyPanel.h | 106   | api,nonthrowing                    | 🚩 | Presents one tick of the declarations into the rectangle the desk resolved for it. learns what a property is. adds a parameter is presented without this file being edited, and a tool presented by hand-written panel code is a tool the panel must be edited to add. write refuses rather than correcting. A row that wrote first would present a refusal on every drag that touched an endpoint. picking interaction `74` owns; a control invented here could only offer an ordinal the artist cannot map to anything they can see.
    in    Theme           const ThemeSpecification&  [-]   read by const reference; no colour or extent is spelled in this component
    in    Area            const WorkspaceRectangle&  [px]  the interior the panel layer handed it, header band included
    in    PresentContext  void*                      [-]   a `PropertyPanelContext*`; a null context presents an empty state
    out   -               void                       [-]   ?
    by    Source/PropertyPanel.cpp, Source/TexturePaintSpecification.cpp
    note  🔴 Matches `PanelPresentRoutine` exactly so a workspace declares it into `PanelIndex` and the desk never
    note  🔴 `76` §4's whole point: the panel presents whatever is declared and names no property. A tool that
    note  🔴 A row **bounds and then writes** — `PropertySpecification.h` requires exactly that order, because a
    note  ⚠️ An `Occupant` measure is presented as a reading and offers no editor. Choosing an occupant is a

F SLATE_DECLARES_PRECISION | PropertyPanel.h | 112   | -                                  | -  | ?
    in    Bounded  PrecisionGuarantee::  [-]  ?
    in    Bounded  PrecisionGuarantee::  [-]  ?
    in    Exact    PrecisionGuarantee::  [-]  ?
    by    Api/AnalyticProjection.h, Api/AssetInterchange.h, Api/AssetPanel.h, Api/AtmosphereIntegrator.h, Api/BrushSpecification.h, Api/CameraProjection.h, (+56 more)
