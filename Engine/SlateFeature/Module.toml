# SlateFeature — feature history, sketch declarations and reference resolution above SlateGeometry.

[unit]
name    = "SlateFeature"
product = "StaticLibrary"
subject = [ "Feature", "Reference", "Sketch" ]

[requires]
unit = [ "SlateGeometry", "SlateMath" ]

[external]
include = [ "ExternalPackages/earcut/include", "ExternalPackages/clipper2/CPP/Clipper2Lib/include" ]
declare = []

[link]
unit    = [ "SlateGeometry", "SlateMath" ]
library = []
