//============================================================================================================================================
//                                                              API.SYMBOLINDEX
//============================================================================================================================================
// 🧩 `60`'s two occlusion terms, kept apart — the per-illuminant projection that attenuates the direct term, and the scalar hemisphere that attenuates the ambient.

%format     symbolindex 1.0
%scope      folder
%path       Engine/SlateCompute/Compute/OcclusionProjection/Api
%layer      SlateCompute
%sources    1
%symbols    41
%annotated  24/41
%cost       ✔️ low · 🚩 medium · 🔴 high (cost rises left to right)

//------------------------------------------------------------------------------------------------------------------------
//                                                        SOURCES
//------------------------------------------------------------------------------------------------------------------------

S OcclusionProjection.h | 418 lines | 172df28e | 41 sym | `60`'s two occlusion terms, kept apart — the per-illuminant projection that attenuates the direct term, and the scalar hemisphere that attenuates the ambient.

//------------------------------------------------------------------------------------------------------------------------
//                                                        SYMBOLS
//------------------------------------------------------------------------------------------------------------------------

V AbsentProjection                          | OcclusionProjection.h | 26      | -                             | -  | ?

//------------------------------------------------------------------------------------------------------------------------
//                                                     THE TWO TERMS
//------------------------------------------------------------------------------------------------------------------------

E OcclusionTerm                             | OcclusionProjection.h | 49-54   | contract                      | -  | Which of `60` §2's two terms a value belongs to. darkens a surface in shadow twice: once because the illuminant is hidden, and once because the geometry hiding it also closes the hemisphere. The artist meets that as contact regions that go black while everything around them is correctly lit, and no amount of adjusting either term fixes it. one; this component is the producer that declaration binds, and a bent orientation's only consumer was indirect lighting, which `00` §5.1 declares absent.
    has   Direct     OcclusionTerm  [-]  ?
    has   Ambient    OcclusionTerm  [-]  ?
    has   TermCount  OcclusionTerm  [-]  ?
    note  🔴 They are **never merged into one scalar** — `60` §2. A single occlusion value applied to both
    note  🔴 Both are **scalar**. `18` §7 declares that no bent orientation is produced and that nothing consumes

//------------------------------------------------------------------------------------------------------------------------
//                                                   ONE PROJECTION SET
//------------------------------------------------------------------------------------------------------------------------

E ProjectionShape                           | OcclusionProjection.h | 65-72   | contract                      | -  | What shape one illuminant's projection takes, which follows from its emission shape and nothing else. is a second answer to a question the illuminant already answered, and the two disagree the moment an artist changes a point illuminant into a spot.
    has   SixFaces    ProjectionShape  [-]  ?
    has   Subdivided  ProjectionShape  [-]  ?
    has   SingleCone  ProjectionShape  [-]  ?
    has   SingleAxis  ProjectionShape  [-]  ?
    has   ShapeCount  ProjectionShape  [-]  ?
    by    Source/OcclusionProjection.cpp
    note  📝 Derived from `44` §3's shape rather than declared beside it. A projection shape declared separately

F ShapeOfEmission                           | OcclusionProjection.h | 80-87   | api,nonallocating,nonthrowing | ✔️ | The projection shape one emission shape implies. `ProjectOcclusionSlot` is: a trivial mapping spelled at the derivation site, the rebuild site and the metrics site is one that acquires a fourth case at exactly one of them.
    in    Emission  EmissionShape              [-]  ?
    out   -         constexpr ProjectionShape  [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 `60` §3's table, as one routine. It is trivial and it is a routine for the reason

V ProjectionExtentTexels                    | OcclusionProjection.h | 93      | -                             | -  | ?
    by    Source/OcclusionProjection.cpp

V DirectionalSubdivisionCount               | OcclusionProjection.h | 94      | -                             | -  | ?
    by    Source/OcclusionProjection.cpp

V PenumbraSampleCount                       | OcclusionProjection.h | 95      | -                             | -  | ?

T ProjectionFace                            | OcclusionProjection.h | 102-108 | owning                        | -  | One face of one illuminant's projection. six planes from it; a projection carrying a matrix alone would have to extract them again, and the two extractions are what `Contract/`'s `NearPlaneDepth` exists to keep from disagreeing.
    has   Projected      ViewProjection  [-]  ?
    has   Bounding       FrustumSpace    [-]  ?
    has   NearestPlane   double          [-]  ?
    has   FurthestPlane  double          [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 A `ViewProjection` and not a matrix. `46` §3 derives the reversed-depth arrangement and extracts the

T DerivedProjection                         | OcclusionProjection.h | 116-125 | owning                        | -  | One illuminant's whole projection — every face, and what it was derived against. the switch and `60` §3 declares that an unenrolled illuminant is integrated unattenuated. That is a declared behaviour and not a failure: an artist lighting a workspace with six fill illuminants does not want six projections, and deleting the illuminant is not the alternative they wanted either.
    has   Faces         std::vector<ProjectionFace>  [-]  ?
    has   Illuminant    OccupantIdentity             [-]  ?
    has   Shape         ProjectionShape              [-]  ?
    has   EmissionSize  double                       [-]  ?
    has   ExtentTexels  std::uint32_t                [-]  ?
    has   DerivedAt     std::uint64_t                [-]  ?
    has   RebuildOwed   bool                         [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 One projection per **occlusion-enrolled** illuminant and none for the rest — `44` §2 gives the artist

//------------------------------------------------------------------------------------------------------------------------
//                                                  WHAT INVALIDATES ONE
//------------------------------------------------------------------------------------------------------------------------

E InvalidationSubject                       | OcclusionProjection.h | 137-146 | contract                      | -  | What changed, so that only what it reaches is rebuilt. otherwise. A camera move rebuilds nothing but the directional subdivision, and a paint stroke rebuilds nothing at all — these are the two things the artist does constantly, and a projection set that rebuilt on either is a workspace that stutters while being used rather than while being changed.
    has   IlluminantAmended  InvalidationSubject  [-]  ?
    has   OccupantMoved      InvalidationSubject  [-]  ?
    has   CameraMoved        InvalidationSubject  [-]  ?
    has   RadiantIntensity   InvalidationSubject  [-]  ?
    has   OccupantPainted    InvalidationSubject  [-]  ?
    has   CutoutCoverage     InvalidationSubject  [-]  ?
    has   SubjectCount       InvalidationSubject  [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 `60` §4: a projection is world-referred and is rebuilt when **what it sees** changes, and not

//------------------------------------------------------------------------------------------------------------------------
//                                                    THE PACKED INDEX
//------------------------------------------------------------------------------------------------------------------------

T OcclusionIndex                            | OcclusionProjection.h | 161-213 | owning                        | -  | Which packed component each reaching illuminant occupies, per partition. `18` integrates that set and this projects it, so the two never disagree about which illuminant occupies which position — which is the whole reason this is derived from that index rather than from the population it was derived from. already truncated there. Both truncations are counted and both are reported, because they are different losses: `44`'s drops the illuminant entirely and this one leaves it unshadowed.
    has   Packed                std::vector<PackedPartition>  [-]  ?
    has   TruncatedAccumulated  std::uint32_t                 [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 `60` §3.1: the set that reaches a pixel is `44` §5's `IlluminantIndex` and **not** the population.
    note  ⚠️ The packed capacity is narrower than `44`'s reach capacity, so a partition may truncate here having

F OcclusionIndex::Derive                    | OcclusionProjection.h | 176     | api,nonthrowing               | 🔴 | Derives the packing for every partition of a reaching index. and writing unity into it. A slot spent on an illuminant that casts nothing is a slot the fifth illuminant that does cast something cannot have, and the artist meets that as their key light losing its shadow when they add a fill.
    in    Reaching     const IlluminantIndex&       [-]  `44` §5's index, already derived against this rotation's partitions
    in    Illuminants  const IlluminantPopulation&  [-]  the population, for each reaching illuminant's enrolment
    out   -            Outcome                      [-]  refuses with ContentUnsupported when the reaching index spans no partition
    post  every partition carries a slot per occlusion-enrolled illuminant it can hold, and a truncation count
    by    Api/AttachmentIndex.h, Api/CameraProjection.h, Api/ChartPartition.h, Api/IlluminantPopulation.h, Api/OcclusionScheduler.h, Api/QuadratureIntegrator.h, (+11 more)
    note  🔴 An illuminant **not enrolled for occlusion** occupies no slot at all rather than occupying one

F OcclusionIndex::SlotOf                    | OcclusionProjection.h | 186     | api,nonthrowing               | 🚩 | The packed component one illuminant occupies in one partition. cannot carry it, and with ContentUnsupported where it does not reach it at all partition contributes nothing there and needs no shadow; one that reaches it and truncated contributes its whole direct term unattenuated, and `18` must know which.
    in    PartitionOrdinal  std::uint32_t     [-]  ?
    in    Illuminant        OccupantIdentity  [-]  ?
    out   -                 Outcome           [-]  refuses with ExtentExhausted where the illuminant reaches the partition and the word
    by    Api/DescriptorIndex.h, Source/DescriptorIndex.cpp, Source/OcclusionProjection.cpp
    note  📝 The two refusals are different facts and are spelled apart. An illuminant that does not reach a

F OcclusionIndex::IlluminantAt              | OcclusionProjection.h | 192     | api,nonthrowing               | ✔️ | The illuminant one packed component carries in one partition.
    in    PartitionOrdinal  std::uint32_t  [-]  ?
    in    Slot              std::uint32_t  [-]  ?
    out   -                 Outcome        [-]  refuses with ExtentExhausted where the slot is unoccupied
    by    Source/OcclusionProjection.cpp

F OcclusionIndex::TruncatedCount            | OcclusionProjection.h | 197     | api,nonallocating,nonthrowing | ✔️ | How many illuminants one partition could not pack — the excess `18` integrates unattenuated.
    in    PartitionOrdinal  std::uint32_t  [-]  ?
    out   -                 std::uint32_t  [-]  ?
    by    Api/IlluminantPopulation.h, Api/TransmissionSequence.h, Source/ConsoleHost.cpp, Source/IlluminantPopulation.cpp, Source/OcclusionProjection.cpp, Source/TransmissionSequence.cpp

F OcclusionIndex::TruncatedTotal            | OcclusionProjection.h | 199     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Api/IlluminantPopulation.h, Api/TransmissionSequence.h, Source/ConsoleHost.cpp, Source/IlluminantPopulation.cpp, Source/OcclusionProjection.cpp, Source/TransmissionSequence.cpp

F OcclusionIndex::SpannedCount              | OcclusionProjection.h | 200     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Api/DocumentSession.h, Api/IlluminantPopulation.h, Api/PopulationIndex.h, Api/PrimitiveStructure.h, Api/SceneStructure.h, Api/SurfaceLayerSequence.h, (+10 more)

T OcclusionIndex::PackedPartition           | OcclusionProjection.h | 204-209 | -                             | -  | ?
    has   Occupying       OccupantIdentity[DirectOcclusionCapacity]  [-]  ?
    has   OccupiedCount   std::uint32_t                              [-]  ?
    has   TruncatedCount  std::uint32_t                              [-]  ?
    by    Source/OcclusionProjection.cpp

//------------------------------------------------------------------------------------------------------------------------
//                                                    THE AMBIENT TERM
//------------------------------------------------------------------------------------------------------------------------

T AmbientOcclusionSpecification             | OcclusionProjection.h | 229-236 | nonallocating,nonthrowing     | -  | What the screen-space ambient term is resolved against. with positions reconstructed exactly as `18` §1 reconstructs them and the hemisphere sampled over `02` §6's planar pattern. The pattern is `02` §6's and is never invented here — `64` accumulates `RadianceSurface` across rotations, so a term sampled from a pattern that is not progressive converges to a different value than the one `64` assumes it is averaging. screen does not occlude, and a surface that leaves the workspace edge brightens. `86` does not report it; it is a property of the term, and `00` §5.1's substitution accounting is where it belongs.
    has   SampleRadius    double         [-]  ?
    has   Strength        double         [-]  ?
    has   SampleCount     std::uint32_t  [-]  ?
    has   ExtentDivisor   std::uint32_t  [-]  ?
    has   JitterDeclared  bool           [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 `60` §5: resolved in screen space from `16`'s `DepthSurface`, at **half extent** as `08` §2 declares,
    note  ⚠️ Screen space is a **declared limitation**, not an approximation to be improved silently. What is off

T AmbientOcclusionSequence                  | OcclusionProjection.h | 244-275 | owning                        | -  | The screen-space ambient term's declaration and its own metrics. consuming side and `60` §2 from this one. It is a product rather than a choice because they describe different scales: channel 6 is detail the topology does not carry, and this resolves contact the topology does carry.
    has   Specification  AmbientOcclusionSpecification  [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 The authored channel 6 and the term resolved here **multiply** — `18` §5 states it from the

F AmbientOcclusionSequence::Declare         | OcclusionProjection.h | 256     | api,nonthrowing               | ✔️ | Declares what the term is resolved against. divisor that is not two `OcclusionSurface` at half extent and `Shared/`'s upsample reads four taps against that claim; a third of the extent would need a different tap count, and the two would be declared in two places.
    in    Declaring  const AmbientOcclusionSpecification&  [-]  ?
    out   -          Outcome                               [-]  refuses with ContentUnsupported for a radius or sample count of nothing, and for a
    by    Api/AttachmentIndex.h, Api/BrushSpecification.h, Api/CameraProjection.h, Api/DecalProjection.h, Api/DescriptorIndex.h, Api/DiagnosticExtension.h, (+65 more)
    note  ⚠️ The divisor is refused above two rather than admitted as a quality setting. `08` §2 claims

F AmbientOcclusionSequence::Resolve         | OcclusionProjection.h | 265     | api,nonthrowing               | ✔️ | The extent the term is resolved at, from one display extent. Rounding down leaves the display's last column with no coarse texel above it, and the upsample then reads outside its own target along one edge.
    in    DisplayAlong    std::uint32_t   [-]  ?
    in    DisplayAcross   std::uint32_t   [-]  ?
    in    ResolvedAlong   std::uint32_t&  [-]  ?
    in    ResolvedAcross  std::uint32_t&  [-]  ?
    out   -               Outcome         [-]  refuses with ContentUnsupported for a display extent of nothing
    by    Api/AtmosphereIntegrator.h, Api/AttachmentIndex.h, Api/BrushSpecification.h, Api/DecalProjection.h, Api/DescriptorIndex.h, Api/DocumentSession.h, (+95 more)
    note  📐 Rounded **up** on both ordinates, matching `RenderSchedule`'s own fraction-of-display claim.

F AmbientOcclusionSequence::Declared        | OcclusionProjection.h | 270     | -                             | -  | ?
    out   -  const AmbientOcclusionSpecification&  [-]  ?
    by    Api/AssetInterchange.h, Api/AtmosphereIntegrator.h, Api/BrushSpecification.h, Api/CameraProjection.h, Api/DecalProjection.h, Api/DescriptorIndex.h, (+84 more)

//------------------------------------------------------------------------------------------------------------------------
//                                                      THE METRICS
//------------------------------------------------------------------------------------------------------------------------

T OcclusionMetrics                          | OcclusionProjection.h | 286-294 | nonallocating,nonthrowing     | -  | What `60` reports through `86`. `86` §2 draws that line, and a rebuild count appended once per rotation would bury the one truncation the artist did not expect under a thousand readings nobody asked for.
    has   ProjectionCount      std::uint32_t  [-]  ?
    has   FaceCount            std::uint32_t  [-]  ?
    has   RebuiltThisRotation  std::uint32_t  [-]  ?
    has   RebuiltTotal         std::uint64_t  [-]  ?
    has   TruncatedTotal       std::uint32_t  [-]  ?
    has   UnenrolledCount      std::uint32_t  [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 The truncation is a **Truncation** and is appended; every other row is a Measure and overwrites.

//------------------------------------------------------------------------------------------------------------------------
//                                                    THE PROJECTIONS
//------------------------------------------------------------------------------------------------------------------------

T OcclusionProjectionSpace                  | OcclusionProjection.h | 307-408 | owning                        | -  | Every occlusion-enrolled illuminant's projection, and the two terms `18` attenuates with. term. It **produces** both targets and amends neither. anything is shaded; a term that read shading would be a one-rotation-stale term, and its staleness would be visible exactly when the illuminant moves.
    has   Projections      std::vector<DerivedProjection>  [-]  ?
    has   PackedIndex      OcclusionIndex                  [-]  ?
    has   AmbientTerm      AmbientOcclusionSequence        [-]  ?
    has   Reported         OcclusionMetrics                [-]  ?
    has   StandingCamera   CameraSpecification             [-]  ?
    has   CameraDeclared   bool                            [-]  ?
    has   SubdivisionOwed  bool                            [-]  ?
    by    Source/OcclusionProjection.cpp
    note  🔴 `60` §6: this records at `08` §3 ③ — after `16` produced `DepthSurface`, before `18` reads either
    note  🔴 Nothing here reads `RadianceSurface`. Occlusion is a visibility question and is resolved before

F OcclusionProjectionSpace::Contribute      | OcclusionProjection.h | 318     | api,nonthrowing               | ✔️ | Contributes `08` §3 ③'s recording. one reconstruction of the same depth. Two recordings would reconstruct the position at every pixel twice to write two scalars.
    in    Schedule  RenderSchedule&  [-]  ?
    out   -         Outcome          [-]  refuses with whatever the schedule refused
    by    Api/DisplayProjection.h, Api/IntersectionOutline.h, Api/OverlayProjection.h, Api/ReflectanceIntegrator.h, Api/RenderSchedule.h, Api/SampleIntegrator.h, (+13 more)
    note  📝 Both targets are produced by one recording rather than by two, because both are resolved from

F OcclusionProjectionSpace::DeclareCamera   | OcclusionProjection.h | 331     | api,nonthrowing               | ✔️ | Declares the camera every directional subdivision is sized against. rebuilt at a declared moment on the tick rather than whenever something happened to read a camera that had since moved. `28`'s sun direction is supplied for the same reason and by the same rule. extended shapes are world-referred and see what they saw before the camera moved.
    in    Declaring  const CameraSpecification&  [-]  the presented camera, as `46` holds it
    out   -          Outcome                     [-]  refuses with ContentUnsupported for a camera declaring no valid clipping interval
    post  a directional projection is owed a rebuild only where the camera moved materially
    by    Source/OcclusionProjection.cpp
    note  🔴 Supplied rather than reached for through a held reference, so that a directional subdivision is
    note  ⚠️ A camera move owes the **directional** subdivision and nothing else — `60` §4. Point, spot and

F OcclusionProjectionSpace::Invalidate      | OcclusionProjection.h | 344     | api,nonthrowing               | 🚩 | Records that something changed, so that only what it reaches is owed a rebuild. than refused. They are the two rows an artist triggers constantly, and admitting them here is what lets a caller declare every change it makes without knowing which ones matter — which is the only arrangement where the ones that do not matter stay free.
    in    Declared  InvalidationSubject  [-]   which of `60` §4's rows
    in    Subject   OccupantIdentity     [-]   the illuminant that changed, or the moved occupant's identity; may be undeclared
    in    Extent    PartitionExtent      [mm]  what the change reaches, for the rows that test reach
    out   -         Outcome              [-]   refuses with ContentUnsupported for a subject outside the declared set
    by    Api/SampleIntegrator.h, Source/OcclusionProjection.cpp, Source/SampleIntegrator.cpp
    note  🔴 `RadiantIntensity` and `OccupantPainted` invalidate **nothing**, and both are admitted rather

F OcclusionProjectionSpace::Rebuild         | OcclusionProjection.h | 359     | api,nonthrowing               | 🔴 | Rebuilds whatever the declared conditions owe, and nothing else. face derivation's own refusal ignored, so `86` can present how many of a scene's illuminants cast nothing. An artist who disabled occlusion on their key light six months ago has no other way to find out.
    in    Illuminants      const IlluminantPopulation&  [-]  the population; every occlusion-enrolled member is projected
    in    RotationOrdinal  std::uint64_t                [-]  the rotation rebuilding
    out   -                Outcome                      [-]  refuses with ContentUnsupported before a camera is declared, and carries a
    post  🔴 with nothing owed, nothing is rebuilt and nothing is recorded
    by    Api/AtmosphereIntegrator.h, Source/AtmosphereIntegrator.cpp, Source/ConsoleHost.cpp, Source/OcclusionProjection.cpp
    note  🔴 An illuminant not enrolled for occlusion is **counted** and skipped rather than silently

F OcclusionProjectionSpace::Standing        | OcclusionProjection.h | 365     | api,nonthrowing               | 🚩 | One illuminant's standing projection.
    in    Illuminant  OccupantIdentity  [-]  ?
    out   -           Outcome           [-]  refuses with ExtentExhausted where the illuminant carries none
    by    Api/ByteSpace.h, Api/CameraProjection.h, Api/ChartPartition.h, Api/CodeInterchange.h, Api/CycleScheduler.h, Api/DecalProjection.h, (+78 more)

F OcclusionProjectionSpace::RebuildOwed     | OcclusionProjection.h | 373     | api,nonallocating,nonthrowing | 🚩 | Whether anything is owed a rebuild. so that the ordinary rotation — a camera the artist is orbiting, a stroke they are painting — rebuilds nothing.
    out   -  bool  [-]  ?
    by    Api/AtmosphereIntegrator.h, Source/AtmosphereIntegrator.cpp, Source/ConsoleHost.cpp, Source/OcclusionProjection.cpp
    note  🔴 What the schedule's contributor reads to decide whether ③ records at all. `60` §4's table exists

F OcclusionProjectionSpace::Report          | OcclusionProjection.h | 381     | api,nonthrowing               | 🚩 | Appends `60` §3.1's truncation and declares every measure beside it. ordinal as the subject, so twelve partitions that each truncated present as twelve entries rather than as one with a count of twelve; `86` §6 refuses the second shape by name.
    in    Reporting  ReportSequence&  [-]  ?
    in    Measured   MeasureIndex&    [-]  ?
    in    Sampled    TickPoint        [-]  ?
    out   -          void             [-]  ?
    by    Api/AssetInterchange.h, Api/BrushSpecification.h, Api/ChartPartition.h, Api/CodeInterchange.h, Api/DisplayProjection.h, Api/HardwareMetrics.h, (+32 more)
    note  🔴 The truncation **appends** and the counts **overwrite** — `86` §2. Coalesced by partition

F OcclusionProjectionSpace::Index           | OcclusionProjection.h | 385     | -                             | -  | ?
    out   -  OcclusionIndex&  [-]  ?
    by    Source/OcclusionProjection.cpp

F OcclusionProjectionSpace::Index           | OcclusionProjection.h | 386     | -                             | -  | ?
    out   -  const OcclusionIndex&  [-]  ?
    by    Source/OcclusionProjection.cpp

F OcclusionProjectionSpace::Ambient         | OcclusionProjection.h | 387     | -                             | -  | ?
    out   -  AmbientOcclusionSequence&  [-]  ?
    by    Source/OcclusionProjection.cpp

F OcclusionProjectionSpace::Ambient         | OcclusionProjection.h | 388     | -                             | -  | ?
    out   -  const AmbientOcclusionSequence&  [-]  ?
    by    Source/OcclusionProjection.cpp

F OcclusionProjectionSpace::Metrics         | OcclusionProjection.h | 389     | -                             | -  | ?
    out   -  const OcclusionMetrics&  [-]  ?
    by    Api/ChartPartition.h, Api/PartitionStructure.h, Api/SampleIntegrator.h, Api/SpecularProjection.h, Api/TransmissionSequence.h, Source/ChartPartition.cpp, (+6 more)

F OcclusionProjectionSpace::ProjectionCount | OcclusionProjection.h | 391     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Api/CameraProjection.h, Source/OcclusionProjection.cpp

F OcclusionProjectionSpace::Derive          | OcclusionProjection.h | 395     | -                             | -  | ?
    in    Declared         const IlluminantSpecification&  [-]  ?
    in    Illuminant       OccupantIdentity                [-]  ?
    in    RotationOrdinal  std::uint64_t                   [-]  ?
    out   -                Outcome<DerivedProjection>      [-]  ?
    by    Api/AttachmentIndex.h, Api/CameraProjection.h, Api/ChartPartition.h, Api/IlluminantPopulation.h, Api/OcclusionScheduler.h, Api/QuadratureIntegrator.h, (+11 more)

F OcclusionProjectionSpace::Located         | OcclusionProjection.h | 399     | -                             | -  | ?
    in    Illuminant  OccupantIdentity  [-]  ?
    out   -           std::size_t       [-]  ?
    by    Api/DocumentSession.h, Api/IlluminantPopulation.h, Api/PointerIntersection.h, Api/PropertySpecification.h, Api/ReportSequence.h, Api/SpatialSubdivision.h, (+26 more)

F SLATE_DECLARES_PRECISION                  | OcclusionProjection.h | 413     | -                             | -  | ?
    in    Perceptual  PrecisionGuarantee::  [-]  ?
    in    Perceptual  PrecisionGuarantee::  [-]  ?
    in    Bounded     PrecisionGuarantee::  [-]  ?
    in    Exact       PrecisionGuarantee::  [-]  ?
    by    Api/AnalyticProjection.h, Api/AssetInterchange.h, Api/AtmosphereIntegrator.h, Api/BrushSpecification.h, Api/CameraProjection.h, Api/ChannelPanel.h, (+51 more)
