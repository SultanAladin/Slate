//============================================================================================================================================
//                                                              API.SYMBOLINDEX
//============================================================================================================================================
// 🧩 Physical tile extents, sliced and reclaimed — slot ledger and byte offsets, and never a texel.

%format     symbolindex 1.0
%scope      folder
%path       Engine/SlateCompute/Compute/TileSpace/Api
%layer      SlateCompute
%sources    1
%symbols    16
%annotated  10/16
%cost       ✔️ low · 🚩 medium · 🔴 high (cost rises left to right)

//------------------------------------------------------------------------------------------------------------------------
//                                                        SOURCES
//------------------------------------------------------------------------------------------------------------------------

S TileSpace.h | 135 lines | 0aeb92f8 | 16 sym | Physical tile extents, sliced and reclaimed — slot ledger and byte offsets, and never a texel.

//------------------------------------------------------------------------------------------------------------------------
//                                                        ONE TILE
//------------------------------------------------------------------------------------------------------------------------

V AbsentTile                    | TileSpace.h | 23      | -                             | -  | ?
    by    Api/SurfaceTileSpace.h, Source/StrokeSpace.cpp, Source/SurfaceTileSpace.cpp

V StoredTexelsPerEdge           | TileSpace.h | 31      | -                             | -  | Texels per edge a resident tile actually stores — the tile plus its apron on each side. brush impressions near a tile edge read valid neighbours without a residency test per texel. Four texels covers trilinear filtering plus the widest impression footprint at the levels that are stored. a defect in their brush rather than as a defect in residency.
    by    Source/TileSpace.cpp
    note  🔴 `20` §1: the apron duplicates the neighbouring tiles' border texels so that filtered sampling and
    note  ⚠️ Without it every seam in the domain is visible in the painted result, and the artist reads that as

//------------------------------------------------------------------------------------------------------------------------
//                                                    THE SLOT LEDGER
//------------------------------------------------------------------------------------------------------------------------

T TileSpace                     | TileSpace.h | 45-133  | owning                        | -  | The physical tile slots one surface's residency draws from, and the byte offsets they occupy. `56` is, and a tile is a projection of it. What is held here is which slots are claimed and where each one would sit inside a device extent `06` claims; the texels live on the device and nowhere else. reused in the same rotation is one the device may still be sampling from, and the artist sees another cell's content appear inside theirs for exactly one rotation, which nobody attributes to reclamation.
    has   Standing          std::vector<SlotStanding>   [-]  ?
    has   ReleasedAt        std::vector<std::uint64_t>  [-]  ?
    has   FreeOrdinals      std::vector<std::uint32_t>  [-]  ?
    has   TileBytes         std::uint64_t               [-]  ?
    has   Ceiling           std::uint32_t               [-]  ?
    has   ClaimedSlots      std::uint32_t               [-]  ?
    has   QuarantinedSlots  std::uint32_t               [-]  ?
    by    Api/SurfaceTileSpace.h, Source/SurfaceTileSpace.cpp, Source/TileSpace.cpp
    note  🔴 This holds **no texels**. `20` §5's gate is that no tile is the source of truth for any content —
    note  🔴 A released slot is **quarantined** for the rotation depth before it is reusable — `20` §5. A slot

F TileSpace::Construct          | TileSpace.h | 59      | api,nonthrowing               | 🚩 | Sizes the ledger to a slot ceiling and a declared texel width. refuses a reserved claim rather than granting a smaller one. Deriving it here would mean deriving it from a device this component is forbidden to name.
    in    SlotCeiling    std::uint32_t  [-]  tiles the surface's backing extent holds
    in    BytesPerTexel  std::uint32_t  [B]  the surface's channel set, as a width
    out   -              Outcome        [-]  refuses with ContentUnsupported for a ceiling or width of zero
    post  every slot is free; nothing is quarantined
    by    Api/AtmosphereIntegrator.h, Api/AttachmentIndex.h, Api/ByteSpace.h, Api/CameraProjection.h, Api/CommandSequence.h, Api/CycleScheduler.h, (+43 more)
    note  📝 The ceiling is the caller's, because it follows from how much extent `06` claimed and `06` §3

F TileSpace::Claim              | TileSpace.h | 68      | api,nonthrowing               | ✔️ | Claims one free slot. must evict first, and exhaustion during ordinary painting is residency policy operating as designed. Reporting it would mean the register is never quiet.
    out   -  Outcome  [-]  refuses with ExtentExhausted when every slot is claimed or quarantined
    by    Api/ByteSpace.h, Api/DescriptorIndex.h, Api/ImageSpace.h, Api/RenderSchedule.h, Api/SpanSpace.h, Api/StrokeSpace.h, (+13 more)
    note  🔴 A refusal is **not** a failure — `20` §2.2 and `86` §5. Every slot claimed means the promotion

F TileSpace::Release            | TileSpace.h | 77      | api,nonthrowing               | ✔️ | Releases one claimed slot into quarantine.
    in    SlotOrdinal      std::uint32_t  [-]  the slot
    in    RotationOrdinal  std::uint64_t  [-]  the rotation the release happened on
    out   -                Outcome        [-]  refuses with ContentUnsupported for an unclaimed or out-of-range slot
    post  the slot is unusable until `RecordingRotationDepth` rotations have passed
    by    Api/ByteSpace.h, Api/ImageSpace.h, Api/PopulationIndex.h, Api/SpanSpace.h, Source/ByteSpace.cpp, Source/ImageSpace.cpp, (+7 more)

F TileSpace::Reclaim            | TileSpace.h | 87      | api,nonthrowing               | 🚩 | Returns quarantined slots whose release is older than the rotation depth. immediately is the defect that costs nothing to write and is invisible until a device is fast enough to still be reading the slot.
    in    RotationOrdinal  std::uint64_t  [-]  the rotation now being recorded
    out   -                Reclaimed      [-]  how many slots became free
    by    Api/AttachmentIndex.h, Api/ByteSpace.h, Api/CommandSequence.h, Api/CycleScheduler.h, Api/DepthReduction.h, Api/DescriptorIndex.h, (+49 more)
    note  🔴 This is the whole of `20` §5's deferred reclamation, and it is one comparison. Reclaiming

F TileSpace::ByteOffsetOf       | TileSpace.h | 95      | api,nonthrowing               | ✔️ | Where one slot sits inside the surface's backing extent. may not name it. `06` adds the base; nothing here knows one exists.
    in    SlotOrdinal  std::uint32_t  [-]  ?
    out   -            Outcome        [-]  refuses with ContentUnsupported outside the ceiling
    by    Source/TileSpace.cpp
    note  A byte offset rather than an address, because the extent it indexes is `06`'s and this component

F TileSpace::StoredBytesPerTile | TileSpace.h | 100     | api,nonallocating,nonthrowing | ✔️ | What one tile occupies, apron included.
    out   -  std::uint64_t  [-]  ?
    by    Api/SurfaceTileSpace.h, Source/SurfaceTileSpace.cpp, Source/TileSpace.cpp

F TileSpace::BackingBytes       | TileSpace.h | 105     | api,nonallocating,nonthrowing | ✔️ | What the whole backing extent occupies — what `06` must claim for this surface.
    out   -  std::uint64_t  [-]  ?
    by    Api/ByteSpace.h, Api/ImageSpace.h, Api/SpanSpace.h, Source/ByteSpace.cpp, Source/ImageSpace.cpp, Source/SpanSpace.cpp, (+2 more)

F TileSpace::SlotCeiling        | TileSpace.h | 107     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Api/SurfaceTileSpace.h, Source/SurfaceTileSpace.cpp, Source/TileSpace.cpp

F TileSpace::ClaimedCount       | TileSpace.h | 108     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Api/DescriptorIndex.h, Api/ImageSpace.h, Api/SpanSpace.h, Api/StrokeSpace.h, Source/ConsoleHost.cpp, Source/DescriptorIndex.cpp, (+5 more)

F TileSpace::QuarantinedCount   | TileSpace.h | 109     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Source/SurfaceTileSpace.cpp, Source/TileSpace.cpp

F TileSpace::FreeCount          | TileSpace.h | 110     | -                             | -  | ?
    out   -  std::uint32_t  [-]  ?
    by    Source/SurfaceTileSpace.cpp, Source/TileSpace.cpp

F TileSpace::LedgerConsistent   | TileSpace.h | 115     | api,nonallocating,nonthrowing | 🚩 | 🔍 Whether every slot is in exactly one of free, claimed and quarantined.
    out   -  bool  [-]  ?
    by    Source/SurfaceTileSpace.cpp, Source/TileSpace.cpp

E TileSpace::SlotStanding       | TileSpace.h | 119-124 | -                             | -  | ?
    has   Free         SlotStanding  [-]  ?
    has   Claimed      SlotStanding  [-]  ?
    has   Quarantined  SlotStanding  [-]  ?
    by    Api/CycleScheduler.h, Source/CycleScheduler.cpp, Source/TileSpace.cpp
