# FontArchives

Typefaces the hosts load at startup — `.otf`, `.ttf`, `.woff2`.

No typeface is committed. A font binary is a licensed artefact and does not belong in the repository;
drop the shipped faces here and `[link].carry` places the whole folder beside the executable.
